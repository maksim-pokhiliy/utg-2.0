# ua-checkout — deferred

Carry-forwards: findings/obligations not yet scheduled, with disposition + status.
**Promote here at every gate** — a WARNING that lives only in scratch is not durable and
gets lost.

**Status:** `OPEN` (live) · `SCHEDULED` (assigned to a step) · `CLOSED` (done — kept for
the trail) · `DROPPED` (decided not to).

The DEF-3x/4x ids below are **inherited** from the production-polish ledger
(`../production-polish/deferred.md` stays their canonical home — close them there too
when they close here).

| ID     | One-liner                                                               | Disposition                                                                                                                                      | Status    |
| ------ | ----------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ | --------- |
| DEF-36 | e2e limiter-bucket isolation (rate-limit state bleeds between specs)    | CLOSED by U4: each spec registers a distinct identity in a frozen record in `e2e/support/app.ts` with an import-time uniqueness throw (Next's `??=` on `x-forwarded-for` lets the explicit header win). Note for future specs: the limiter is now LIVE in the battery — an unregistered spec lands in the socket-derived bucket, which is not fail-open | CLOSED    |
| DEF-37 | relay-forwarding e2e (proxy route forwarding never exercised e2e)       | fold into U6 (contract flip proves forwarding)                                                                                                   | SCHEDULED |
| DEF-39 | cart decoder field validation + honest typing                           | fold into U5 (checkout rework touches the same decoder)                                                                                           **CLOSED 2026-08-08 by U5a** — the decoder is total, drops invalid lines individually instead of losing the whole cart, and blocks the `price`-garbage → `total:"NaN"` → relay-400 path | CLOSED |
| DEF-41 | DS Skeleton hidden-state coupling                                       | fold into U3 (the DS window)                                                                                                                     | CLOSED    |
| UAC-1  | NP business-cabinet API key: obtain from the operator, set in Vercel    | **CLOSED 2026-08-08** — key received (business cabinet, valid to 2027-08-08) and set on Vercel project `utg` as `NOVA_POSHTA_API_KEY` (Production, Sensitive). Deliberately NOT redeployed: nothing reads it until U5b, so it binds on the next natural master deploy. Pulled forward from U7 by D-12, and that alone paid for itself — see UAC-13  **CORRECTION 2026-08-08:** this row said the key was "deliberately NOT redeployed ... it binds on the next natural master deploy", which was true literally and wrong in consequence — the next deploy was U5a's merge about an hour later, so the key has been live in production since then, not "when U5b needs it". The reviewer raised it as a merge gate and was right to. Nothing was harmed (the limiter holds for honest clients and no UI consumes the routes), but the exposure existed a day earlier than this ledger implied | CLOSED |
| UAC-2  | NP response fields re-verified against the official portal              | U4 re-check DONE via five substitute sources incl. captured API fixtures (the portal 403s, legacy devcenter dead) → three §4 corrections under D-8. **Residual live-key proof DONE 2026-08-08** (D-12, pulled forward from U7): string `Page`/`Limit` accepted live; `searchSettlements` returns both `Ref` (SettlementRef) and `DeliveryCity` (CityRef) and the proxy correctly surfaces `DeliveryCity`, so warehouse lookups address the right identifier space; labels arrive as `Present`-style (`м. Київ`, `с. Київське`) with `region` already joining district + oblast (`Новомиколаївський р-н, Запорізька обл.`); warehouse labels are `Відділення №N: вул. …`, which B4's sanitizer renders as `Відділення NoN: вул. …` — the `№ → No` fold verified against real carrier data, legible. Kyiv's own region reads `Київська обл.`, so UAC-9's rejoin yields `м. Київ, Київська обл.` — redundant-looking but correct; do not "fix" it. The multi-page Kyiv merge was attempted and FAILED — see UAC-13 | CLOSED |
| UAC-3  | uk name placeholders in the ratified prototype dict are still John/Wick | U5 dictionaries use real UA examples per requirements §6 (e.g. Марія / Шевченко); no design impact                                                **CLOSED 2026-08-08 by U5a** — uk placeholders are Марія / Шевченко | CLOSED |
| UAC-4  | The Claude Design kit (`62bf007e`) lags the repo DS                     | after U3 merges: audit the full kit↔repo-DS delta (the U3 four + any 4d-era additions never backported) and backport in one `/design-sync` pass | OPEN      |
| UAC-5  | Combobox panel has no row budget (~23ms/hover at 1000 rows, measured)   | CLOSED by U4 (D-7): the proxy filters from its own cache and caps every response at 30 warehouse / 10 settlement rows, pinned by route units     | CLOSED    |
| UAC-6  | `CartDrawer` `sizes="64px"` under-hints the stretched ~72–92px frame    | one-token consumer-side fix (`sizes="96px"` per the deep review's DPR math), rides U5                                                        **CLOSED 2026-08-08 by U5a** — `sizes="96px"`, and the checkout summary's own CartLine inherits it | CLOSED |
| UAC-7  | Combobox adoption watch-list for U5's browser gate: panel `z-30` sits under the sticky header `z-40` when the anchor input scrolls beneath it (ruled not-a-defect, D-6.3); pointer can steal the active row from keyboard scroll (bounded to one steal) | watch at the U5 browser gate; promote to fixes only if they bite there (the Enter-during-debounce item was FIXED in the U3 review round — D-6.1 superseded prototype parity). PLUS the U5 integration contract from D-6.2: the combobox acknowledges a search via `loading` flip OR `options` identity change OR emptied query — a consumer that answers async, never raises `loading`, and returns a referentially identical array shows loading bars forever; and inline-built `options` arrays reset the active row on every parent re-render | SCHEDULED |
| UAC-8  | DS hygiene pair deferred from the U3 deep review: chip cva is forked from `SizeSelector` instead of promoted (RF-15); the 300ms settle fade is duplicated between `Skeleton` and `ReportsScreen` (RF-19) | one DS-hygiene window together with the UAC-4 kit backport: unify the chip base (SizeSelector composes it, gains its first tests), single fade-duration source (theme was frozen in U3)                | OPEN      |
| UAC-11 | The per-IP limiter can be disabled by the client: a request carrying EMPTY `x-forwarded-for` and `x-real-ip` makes `resolveClientKey` return `null`, and the limiter fails open by design (measured locally by the PR #19 reviewer: 65 forged requests, zero 429s; the same applies to `place_order`'s 5/60s budget). **Stakes rose in U0/B2**: once the relay enforces the secret, this limiter is the sole gate in front of an authenticated path — a caller who forges the identity headers can spend the shop's own credential without bound | PRE-EXISTING on master, not introduced by U4 (the code moved verbatim). Whether it reaches prod depends on Vercel's edge: if the platform overwrites client-supplied identity headers the hole is already closed there. NOT verifiable on the PR preview — Deployment Protection 302s every request before it reaches the route. Verify at the U7 prod gate against the live GET route (cheap recipe: ~62 requests to `/api/np/settlements?q=…` with `-H 'x-forwarded-for;' -H 'x-real-ip;'` — a 429 anywhere proves the platform rewrites, all-200/503 proves the hole is live). If live: switch fail-open to a socket/derived identity for the directory bucket only — `place_order` MUST stay fail-open (a false 429 costs a real volunteer order)  **HALF-ANSWERED 2026-08-08, and RESCHEDULED from U7 to BEFORE PR B MERGES.** The limiter is confirmed alive in production against an honest client: 59 × 200 then 429 with `retry-after: 34`, probed on a below-min-length query that costs the carrier nothing. The forged-identity half is still open — it needs a run with empty `x-forwarded-for`/`x-real-ip`, which the planner's own safeguard blocks as rate-limit hammering, so the owner runs it. Why it moved: the operator's paid key is LIVE in production (see UAC-1's correction) and PR B is what brings real buyer traffic to the directory; answering this at the U7 gate would be answering it after it mattered  **CLOSED 2026-08-11 — THE HOLE IS NOT LIVE.** Pacing-immune probe against the live route (70 URLs through ONE curl invocation, so the requests land inside the 60s window instead of trickling at ~1/s): the forged-identity run returned **60 × 200 then 10 × 429**, and the honest control run was byte-identical — 60 × 200 then 10 × 429. Vercel overwrites `x-forwarded-for` exactly as its docs state ("Vercel overwrites this header and does not forward external IPs to prevent spoofing"). No change to `place_order`'s fail-open identity is needed. **The first attempt was inconclusive and the planner read it wrong, which is the part worth keeping:** two SEQUENTIAL serial-curl runs share one sliding 60s window, so the "honest control" was contaminated by the forged run's own consumed slots, and serial curl at ~1 req/s never fills a 60/60s bucket at all. The planner announced "the hole is live" from `forged: no 429 / honest: 429 at 21` before checking whether window arithmetic could produce that same shape with NO hole. It could, and it did. **Two sequential runs sharing a rate-limit window are not a treatment and a control** | CLOSED |
| UAC-12 | `place_order` buffers the whole upstream body with `response.text()` (an 8 MB body was measured re-emitted in full) and forwards the upstream `Content-Type` verbatim with no allowlist and no `nosniff`, so a hostile upstream `text/html` passes through as-is | PRE-EXISTING and byte-identical on master — surfaced by the PR #20 review, not introduced by it. Real only if the relay itself is compromised or `PLACE_ORDER_URL` is repointed. Fix when the order path is next open (U5a flips the payload there): cap the buffered body and pin the response `Content-Type` to `application/json` + `nosniff` instead of echoing. Good news measured on the same surface: CRLF injection through the upstream `Content-Type` is already neutralized, and the relay's own response bodies are module-level constants that cannot carry request data | SCHEDULED |
| UAC-10 | NP-proxy polish pack from the PR #19 deep review                         | U5 window: Present edge guards (a malformed `Present` can yield an empty label — reachability practically nil, NP composes `Present` itself; the per-side 256 label cap breaks the label+region rejoin only beyond 256 chars/side — real NP maxes ~90); `server-only` import boundary for the NP client (the repo lacks the package — needs the dependency or a build alias; the boundary goes live when U5 adds client callers; today the key provably stays in server chunks)  **CLOSED 2026-08-08 by U5b PR A** — `Present` edge guards done incl. the degenerate-region case the review caught, and the `server-only` boundary is live (a client component importing the carrier client fails `next build`, proven by the reviewer) | CLOSED |
| UAC-9  | U4→U5 integration handoff pack (D-8 consequences for the checkout rework) | U5 must: rejoin `delivery.city` as `label + ", " + region` (never `label` alone); treat only non-OK responses as capability-down (a blank `q` legitimately answers 200-empty — not a fallback trigger, and a genuinely empty result set — postomat-less village, all-non-Working city — is a normal 200-empty too, so the combobox needs a manual-entry path even in live mode); every NEW e2e spec registers a per-spec identity in the frozen `e2e/support/app.ts` record (DEF-36 made the limiter live in the battery — an unregistered spec silently lands in the socket-derived bucket, which is NOT fail-open); U5 contour clarifies `source` semantics for `np_courier` (street stays manual in MVP per §2 — `source: "np_directory"` speaks to the city only); browser-gate watch: «м.»/«с.» prefixes now sit inside `label` and village regions are two-part («…р-н, …обл.») in the right-aligned mono-caps meta slot — overflow risk | SCHEDULED |

| UAC-13 | **The NP warehouse page-merge cannot complete against the live API.** Page 1 for Kyiv returns `success: true` with 500 rows; page 2, fired immediately after, returns HTTP 200 with `success: false`, `errors: ["To many requests"]`, `info: ["Try again after 0.5 seconds"]`. The proxy correctly collapses that to one 503, so the route fails OPEN to free-text — but it means **every city needing more than one page silently loses the directory**, Kyiv first of all and by inference Харків / Одеса / Дніпро / Львів: exactly where the orders are. Single-page cities work end to end (verified with Ірпінь: real branches, real labels) | Found 2026-08-08 by the D-12 pre-step probe, with the operator's real key against a local dev server — U4's fixtures never rate-limit, so its tests are green and always will be. **FIRST task of U5b, before any UI consumes the route.** The fix is to pace or retry the pages honoring NP's own "try again after 0.5 seconds" hint inside the existing 7s merge budget; do NOT raise `Limit` (500 is NP's cap) and do NOT widen the 503 into a partial result — half a warehouse list is worse than free-text, because the buyer cannot tell it is half **CORRECTED 2026-08-08 by a pre-step probe (D-14) — the prescription above is WRONG and is kept only as the trail.** Pacing at 600ms does clear NP's rate limit (pages 1–8 all succeed), but Kyiv reports **12 298** warehouse points in its own `Warehouses` count — about 25 pages against a 10-page cap — and page 9 dies on OUR 7s deadline, not on NP's limit. D-8's "Kyiv ≈ 3000 warehouses" estimate was low by 4×, so the whole-city merge was never completable at any budget. The real fix is architectural: delegate the substring search to the carrier via `FindByString` (measured: one page, 0.9–3.2s) and delete the page-merge and the 24h city cache entirely — D-14  **CLOSED 2026-08-08 by U5b PR A** (`25c58d7`) — the search is delegated to the carrier, the merge and the city cache are gone; prod-verified: the Kyiv branch query that answered 503 an hour earlier returns real відділення | CLOSED |


| UAC-14 | The in-flight submit lock has a second, unlocked door: the header cart drawer is mounted in the root layout, live on `/checkout`, and its `CartLine` gets no `locked` prop. A line removed mid-flight vanishes from the UI but still ships, and the buyer is told success | PRE-EXISTING on master. The U5a review refuted the serious harms by execution — the payload is snapshotted before the await, so the in-flight request is not corrupted, there is no React fault, no stuck pending and no lost success screen. Bounded to the in-flight window. Note also that §9's rationale clause ("the order must not mutate mid-flight") overstates what the summary-scoped lock achieves; §9's normative clause is summary-only. Natural window: U5b, which reopens the checkout | OPEN |

| UAC-15 | `CheckoutForm.tsx` is now the largest component in the repo — 315 LOC against the manifesto's 300 bar (previous max was `ProductScreen.tsx` at 237) | **SCHEDULED → U5b, BEFORE the Нова Пошта block is added**, not after. U5b adds an entire delivery-method surface to this same file; extracting the customer and delivery blocks costs nothing today and is a rewrite once the NP block is in. Both extractions are clean and presentational | SCHEDULED |

| UAC-16 | Checkout polish pack from the U5a review, none a defect today, each verified unreachable or cosmetic: **(1)** the submit re-entrancy guard is state, so it is a render behind — three synthetic submits in one `act()` produce three POSTs, unreachable in a real browser since `Button loading` carries a real `disabled`; a ref would be honest, and `idempotencyKey` already is one; **(2)** `pending` is released after the `await` rather than in a `finally`, with no reachable thrower today, but the blast radius grew from a spinning button to every input and the whole summary, so a stuck pending is now unrecoverable without a reload; **(3)** focus falls to `<body>` when the focused input is disabled at submit; **(4)** the remove-confirm flow is duplicated verbatim between `CheckoutSummary` and `CartDrawer`, and the accessible-name templates now live in three places including `e2e/support/app.ts`, so a label change breaks e2e selectors rather than an assertion; **(5)** test nits — names that overclaim, a pin on a path gated out upstream, `UUID_V4` written three times, `NORMALIZED_PHONE` hand-derived from the dictionary instead of read from it; **(6)** the `top-[76px]` sticky offset is the only arbitrary breakpoint in `src/` (header is 66px) | Batch into the U5b window rather than growing an already-large PR. (4) is at the manifesto's extraction threshold and pairs naturally with UAC-15 | OPEN |

| UAC-17 | Two invisible-character patterns now exist: `src/utils/phone.ts` strips the complete `\p{Cf}` class, `src/app/api/np/settlements/directory.ts` strips a hand-listed subset that MISSES U+061C and the three isolates U+2066–U+2069 | Deliberately not unified in U5a — the NP surface was fenced to U5b, where that file is open anyway for UAC-13. Unify then, taking the complete class; a partial strip on the carrier's own labels is exactly the bidi-spoofing hole the relay's B4 sanitizer closes from the other side  **CLOSED 2026-08-08 by U5b PR A** — one `stripInvisibles` on the complete `\p{Cf}` class (the retired hand-list missed 158 codepoints), placed inside `readString` so every carrier-derived field is covered by construction rather than by memory | CLOSED |


| UAC-18 | U5b **PR B** carry-forwards from the PR A review, all three about the cost of delegating the search: **(1)** the negative cache lost its outage damping — the key went from `cityKey` to `city\|method\|query`, so cardinality is now O(cities×methods×queries) and a measured 6-keystroke session during a carrier outage produced 6 upstream attempts against U4's 1, each locking its own key into 503 for 30s, stranding a whole typing session in free-text instead of one city; **(2)** nothing bounds upstream fan-out now that `MAX_CONCURRENT_MERGES` died with the merge (measured: 40 distinct queries → 40 simultaneous calls); **(3)** flipping the delivery chip re-downloads a byte-identical page, because `method` is in the cache key but never in the upstream body | (1) is the sharpest and pairs with PR B's debounce — they are the same problem, solve them together. (3) is fixable by caching the decoded page under `(city, query)` and splitting on read, with the entry cap coming down as entry size grows  **CLOSED 2026-08-11 by PR #23.** All three paid, each reproduced before it was fixed: (1) the damper cut a 6-query outage session from 6 upstream calls to 3, and to 0 for every buyer inside the cooldown; (2) 40 simultaneous queries now draw 12 concurrent calls, the other 28 refused *while those 12 were still in flight*; (3) the chip flip draws 1 call where it drew 2. One correction to this row's own disposition, measured against the real DS `Combobox`: the 250ms debounce collapses BURSTS, not SESSIONS — at any cadence slower than ~4 chars/s, i.e. anyone typing Cyrillic on a phone, it dispatches one search per keystroke. So "(1) pairs with the debounce" was wrong; the damper carries it alone. Rulings D-16/D-17/D-18 | CLOSED |

| UAC-19 | PR A review ledger set, none a defect today, each verified by execution: **(1)** empty answers are never cached while the sibling settlements module caches its empties — D-8 ¶4's rationale ("an attacker-supplied ref would evict the real cities for a day") has expired now the TTL is 5 minutes, so the asymmetry needs re-ratifying rather than inheriting; **(2)** a malformed settlement container reads as "no results" and is positive-cached, so a rename of one key would read as "this place does not exist" and never trip the fallback — warehouses closed exactly this hole in U4, settlements never did; **(3)** one malformed row on a narrow query is a whole-query 503, because "all rows failed to decode" was a strong signal over 500 city rows and fires on one bad row over an N=1 result; **(4)** the cache key joins on an unescaped `\|` — collision reproduced, but a refuter proved by separator algebra that no attacker triple can collide with a real buyer's key, so it is attacker↔attacker only AND the proof collapses if PR B ever passes a composite `city`; **(5)** a query matching >500 raw rows draws its 30 from a window rather than the match set (precondition unobserved — measured queries returned 0/2/3/11/65/361) | Batch into the U5b PR B window or U6. (4) carries a live condition worth re-checking when PR B lands.  **(4) CLOSED 2026-08-11 by D-18** — and it was worse than this row recorded: dropping `method` from the key opened an attacker↔BUYER collision, not attacker↔attacker. Fixed by length-prefixing, proven injective over 1 126 366 round trips. **(3)'s cost ROSE** and needs re-pricing: "all rows failed to decode" was a weak signal over 500 city rows and is a strong one now a page is often a single row, and the resulting 503 is both sticky and negative-cached for 30s. (1), (2), (5) still OPEN | OPEN |

| UAC-20 | The directory routes never link the CLIENT's abort to the upstream fetch, so every abandoned keystroke keeps burning a carrier call to completion. It is the real multiplier behind UAC-18(1) and (2) — the fan-out cap bounds its damage but does not remove it | Deferred deliberately at the PR #23 plan gate, planner-approved: wiring it is a semantics change, because an aborted request would stop populating the cache and would poison single-flight joiners. Stated in the PR body so a reviewer does not spend a finding rediscovering it | SCHEDULED → U6 |

| UAC-21 | PR #23 first-review tail, each verified by execution, none blocking: **(1)** `INITIAL_DELIVERY`/`DeliveryValues` are dead and 7 assertions pin them — flipping `INITIAL_DELIVERY.method` reddens 1 test and changes nothing shipped, flipping the real `useState` initializer reddens 46, so those tests read as guarding the form's opening state and do not; **(2)** manifesto items — `useNpDelivery` is a 209-line function, the largest in `src/`, so UAC-15's file-size breach partly reappeared next door; `ERROR_KEYS` is byte-duplicated across two component files (half of one copy unreachable); `FIELD_PAIR` is exported from one component to another; `MethodAvailability`'s `hasWarehouses`/`hasCourier` are false in the fall-open branch that derives the buyer-visible reason; **(3)** the client's `readString` does NOT strip `\p{Cf}` while the server's identically-named function does — same name, different guarantee (defence-in-depth only, our proxy strips), and no `maxLength` anywhere in the checkout, so a 5000-char city reaches the POSTed body (the relay clamps and sanitizes at the far end, verified in `../utg-tg-order-bot/src/message.ts`); **(4)** `Combobox.pick()` clears `blurRef` but not `debounceRef`, so selecting within 250ms fires a search after selection | Deliberately out of scope of a PR already at 54 files. Natural window: U6, or the next checkout-touching step | OPEN |

| UAC-22 | PR #23 first-review below-the-cut set, 11 distinct, named not detailed: invented uk copy `np_fallback_hint_warehouse` that §6 does not dictate · settlements never shape-guard the ref where it is minted (drift-only, refuted as unreachable today) · `guardCarrierCall` skips `recordOutcome` when `attempt()` throws · a Cargo-only page is never cacheable so every keystroke re-hits the carrier · `e2e/uk-delivery.spec.ts:109`'s name claims a re-query its body disproves · client `readCount` accepts NaN/Infinity where the proxy guarantees integer ≥0 · `payload.test.ts` asserts on a literal the test file wrote · ~8 no-op `.textContent` assertions · `MIN_QUERY_LENGTH` duplicated client/server · `cache.test.ts` asserts `f(a,b) === f(a,b)` | Recorded so the tail is durable rather than lost with the review. Hygiene; no window assigned | OPEN |

| UAC-23 | PR #23 re-review ledger set, each verified by execution, none blocking: **(1)** `client.ts` classifies `success:true` with a non-array `data` as carrier DISTRESS. The planner RULED it stays (D-17) — a carrier whose successful responses we cannot parse is one we cannot serve a buyer from, so damping costs nothing; the re-reviewer disagreed, holding D-17's conservative direction admits no exception. **Re-read this row the day Нова Пошта changes a response shape** — the failure mode is a 30s directory blackout for every buyer; **(2)** the blur probe is not deduplicated — `searchCity` only short-circuits when `cityChoice !== null && query === cityChoice.label`, and in free-text mode `cityChoice` is always null, so an unchanged query re-fires on every blur (measured 2 route hits per blur, 4 per round trip when alternating the two fields). Carrier-safe — zero upstream amplification, verified — and invisible to the buyer, since a 429 in free-text mode renders nothing; **(3)** after the city un-latches, the always-mounted `aria-live` region re-announces «Довідник Нової Пошти зараз недоступний» at the moment it became available, because `warehouseSource` stays manual until `selectCity` or a warehouse success; **(4)** a `response.json()` throw is retried while a wrong-shaped 200 body is not, though both are "our own route answered 200 with something unusable"; **(5)** `pause()` is not abortable, so an abandoned request holds a 400ms tail and defers `clearTimeout` on the 10s deadline; **(6)** `describedBy` is threaded into the directory branch of `CheckoutDirectoryField` and silently ignored — `Combobox` builds its own `aria-describedby` | (1) is a live decision to revisit on carrier drift, not a defect. (2)–(6) are polish. Natural window: U6 | OPEN |

| UAC-24 | One line in `CheckoutDirectoryField.tsx` is deliberately kept and deliberately UNPROVABLE: `wasFocusedRef.current = !event.currentTarget.isConnected`, the guard stopping a blur fired BY the node's own removal from clearing the "this field held the caret" flag. **Measured 2026-08-11: neither jsdom NOR Chromium fires `blur` when a focused node is removed** — jsdom probed directly (`input.focus(); input.remove()` → no blur, `activeElement` becomes `BODY`), Chromium probed by mutation (the whole 30-spec browser suite stays green with the guard destroyed). The branch is unreachable from every environment this repo can run | Firefox DOES fire blur on removal, and without the guard a Firefox buyer loses the caret exactly as Chromium did before `940a63b`. Keeping a known regression out of an untested browser beats deleting a line to satisfy a coverage table. The only way to cover it is a Firefox project in `playwright.config.ts` scoped `testMatch: /uk-delivery-focus/` — declined for now (a second browser binary in the CI cache and install time on every PR forever, to cover one defensive line whose failure costs keystrokes, not orders). **Ride it along the day Firefox coverage is added for any other reason.** The PR body carries this under its own heading with a mutation-table row reading "nothing — see below" rather than one implying coverage | OPEN |


## Detail on the live ones

- **UAC-4** — user-directed 2026-08-05 («пишем в debt»). The kit project is the
  visual origin/donor; the repo DS is the implementation canon; design prototypes
  hand-roll what the kit lacks (by design — same mechanism as the 4d-era
  ProductCard), so the kit accumulates lag. The backport keeps future design
  sessions composing real components instead of re-inlining them. Scope: Combobox,
  ChoiceChips, CartLine amendment, Skeleton settled state, plus whatever the delta
  audit finds from the 4d era.

- **UAC-1** — the live-autocomplete acceptance criterion needs a real
  `NOVA_POSHTA_API_KEY` in Vercel env before the U7 prod gate. Everything up to U7
  ships and verifies without it (fail-open fallback is a first-class mode, D-1.4). If
  the key slips, U7's live-autocomplete check moves to a follow-up verification — the
  initiative does not block on it.

## Closed history

- **DEF-36 + UAC-5** (2026-08-06, U4 / PR #19) — e2e specs carry per-spec
  rate-limit identities (frozen record + import-time uniqueness throw), and the
  directory row caps live at the proxy where the server decides how much of the
  carrier's directory reaches the client. DEF-36 closed in the production-polish
  canonical ledger the same day.

- **DEF-41** (2026-08-05, U3 / PR #18) — `Skeleton` owns its settled state
  (`settled` prop: fade + pulse-stop internal); `ReportsScreen` dropped the
  `animate-none` knowledge of DS internals; `grep animate-none src/components/`
  is empty and unit+e2e guarded. Closed in the production-polish canonical
  ledger the same day.
