# production-polish — journal

Append-only. One entry per session/step.

## 2026-07-17 (evening) — Firebase outage discovered, de-Firebase pivot, content rescued, step-0 plan approved

- User directed dropping Firebase ("проект отлично ездит на Vercel"); investigation revealed prod is partially down: Storage 402s every image (Spark plan stopped serving; user can't even export without paying), `GET /api/categories/[id]` 504s reproducibly. Only the top-level categories endpoint works.
- Content rescue: public `utg-1.0` GitHub repo contained ALL images (products, reports, about, favicon, logos) **plus the full catalog as static config** — richer than Firestore (descriptions, sizes, report captions). UTG logo recovered from Wayback (640px via Vercel optimizer cache). Only gap: home hero photo (DEF-9). Everything staged in `extracted/` with inventory README.
- Order-bot source recovered (public `utg-tg-order-bot` repo → `telegram-bot-server` on Vercel) — DEF-2 CLOSED: contract fully documented; current client payload matches it exactly.
- D-7 RATIFIED: no Neon — static typed catalog in repo (data never changed in project life), images in `public/`. Step 1 rescoped to Rescue/de-Firebase + RSC; charter acceptance criteria updated (zero Firebase footprint, env → 3 keys).
- Step-0 executor plan reviewed: on-spec, scope fence respected, honest verification plan. Its gate question (money fallback) resolved as option X = coefficient→1 (D-8); the wrong-symbol corner case explicitly reassigned to step 1's gates.
- New deferred: DEF-9 (hero), DEF-10 (t-shirt availability confirm), DEF-11 (CartItem quality={0}, executor finding).
- Blaze-window harvest cancelled: Firebase demands $30 prepay to unseal Storage; user filed a support ticket. Confirmed non-blocking — all step-1 inputs are already recovered; DEF-9 stopgap plan stands, DEF-10 falls back to user memory.
- DEF-10 CLOSED: user confirmed availability flags (t-shirts all out of stock, patches+stickers in stock).
- Design track added on user's initiative: D1 (system primitives) + D2 (screen prototypes) via Claude Design, running parallel to code steps 1–3, gating 4a–4c. Briefs written (`design-1-primitives-prompt.md`, `design-2-screens-prompt.md`). D-4 ratified at brief level (keep + sharpen brutalism); final gate = prototype review. D-9 RATIFIED: shadcn/ui on Tailwind as component layer (user delegated; retires Flowbite, notyf, body-scroll-lock, swiper).

## 2026-07-17 (night) — Step-0 PR planner-approved; step-1 prompt issued

- Step-0 PR #2 (7 commits, 18 files) reviewed by executor's max-level code review (2 real bugs found+fixed: 204-null-body crash, rate=0 pass-through) and an independent reviewer (approve, 4 minor findings). Planner re-verified independently: file list clean of planner artifacts, all key diffs read, `--frozen-lockfile` consistent, tsc/lint green, `yarn build` green with no `.env.local`, zero `setTimeout` left. Verdict: approve; merge delegated to user (squash recommended).
- Reviewer findings routed: details-leak → DEF-12 (scheduled step 1), rates-frozen-at-build fact → folded into step-1 revalidate work (DEF-1), $-glyph corner → already D-8 (step 1 is SSOT, not "step 2" as PR text says), coefficient-default nit → dies with Recoil in step 2.
- Bonus recovery: 1.0 repo carries **Ukrainian translations for product titles + t-shirt descriptions** — copied to `extracted/translations-1.0-{ua,en}.json`; the step-1 static catalog becomes properly bilingual (Firestore never was).
- New: DEF-13 (rates-down en checkout → `$` on UAH total in operator Telegram message; coordinated payload+bot fix later — bot repo is user's own).
- `step-1-rescue-prompt.md` written: static catalog + local images + RSC + de-Firebase + D-8 terminal money fix + DEF-8/DEF-12 + cart-key bump (stale carts hold dead firebasestorage URLs that would crash next/image after remotePattern removal).
- PR #2 squash-merged by user (`04fbbf9`), remote branch pruned. Step 0 CLOSED. CLAUDE.md refreshed to post-step-0 reality (env-tolerant boot, no setTimeout note, Vercel prod noted). Step 1 unblocked.

## 2026-07-17 (late night) — Claude Design delivered D1+D2 in one shot; planner file-review done

- User authorized DesignSync (`/design-login`); project `62bf007e-1ea9-45bc-a40a-f64544314e8c` "UTG Design System" connected (planner's stray `claude mcp add` reverted — DesignSync is the sanctioned interface).
- Claude Design consumed BOTH briefs from the uploaded folder (its readme cites `design-1-primitives-prompt.md`/`design-2-screens-prompt.md` + decisions D-4/D-9) and generated: full token set (shadcn CSS-var aliases, hex, light theme), 19 components (core/forms/commerce/layout incl. Icon wrapper), 17 guideline cards, vendored fonts, and a clickable bilingual storefront kit (all D2 screens except 404; EN via live switcher; About lives in Reports.jsx).
- Planner file-level review (readme, colors.css, typography.css, data.js, kit README): direction absorbed faithfully — warm paper #FFF9F6 / warm ink #181512, border=ink, ring=flag-blue, radius 0, claimed-AA pairs; type = Oswald 600 caps (display) + IBM Plex Sans (body) + IBM Plex Mono caps (meta/prices — "paperwork voice", smart addition); Lucide icons 2px stroke; amateur-photo treatment (1:1, 2px ink frame, out-of-stock 45% grayscale). Honest-tone guardrails held (no invented report captions).
- Data-faithfulness punch list issued to user for the design session: (1) «With You» patch is actually titled «Death» in the 1.0 catalog (only real violation); (2) uk tee titles must use Чорна/Зелена/Сіра per recovered translations, not English color words; (3) set title verbatim "Set of «Waiting, Welcome, Death»", not "Set of Three Patches"; (4) «Death» tees are missing their descriptions (1.0 has all six). Optional: 404 (DEF-15).
- New: DEF-14 (kit's good new UI copy → real dictionaries in 4b/4c), DEF-15 (404 design gap).
- Visual ratification of D-4 remains with the user in the Design UI (planner reviewed files, not pixels).
- User challenged the punch list ("текст не важен, вставим свой"); planner conceded the data-as-data half (items 1–2 dropped — implementation reads `extracted/`, not design `data.js`) and held the design-load half: prototype text is the layout's test data — the catalog's longest title (set) and longest descriptions (out-of-stock «Death» tees) must exist in the prototype or the review validates nothing. User accepted the trimmed 2-item list.
- Design session applied it: set title verbatim, all six tee descriptions, FPV caption re-anchored to report 3, «Death» patch self-corrected from source (the dropped item fixed itself via verbatim discipline), 404 built → DEF-15 CLOSED. Remaining: uk tee descriptions (the mount predates the translations extraction — planner process gap; Desktop copy refreshed, strings handed over via chat).

## 2026-07-17 (later) — Step-1 Gate A approved with one amendment

- Executor's research/design/plan reviewed: localized-views catalog module (canon {uk,en} → server resolves → flat views via props; category names move out of `dictionary.categories`), App-Router-literate 404 design (`dynamicParams=false` on `[lang]`, root-not-found fallback only if the build proves it needed — preserves per-locale `<html lang>`), faithful uk patch titles (= English strings, none invented), Reports/About stay client with URL swaps only, LoadingContainer deleted (dead after RSC), framer-motion orphan flagged not removed (4a), good pre-existing find: cross-locale money staleness (locale switch keeps the old coefficient — Recoil initializeState runs once; step-2 fixes).
- Amendment: copy ALL 13 product photos incl. `stickers1.JPG` — unused by the 1.0 catalog but referenced by the ratified design kit (sticker product card), and `public/` becomes the asset SSOT once `extracted/assets/` is deleted post-step-1; shipping 12 would strand 4b.

## 2026-07-18 — Step-1 PR #3 planner-approved

- Executor delivered 11 atomic commits (incl. stickers1 amendment); its max code-review fixed 4 findings (favicon middleware guard — real, self-introduced; summaries-vs-views payload slimming; dedup; Promise.all) and correctly rejected 4 with initiative context. Independent reviewer: approve, 5 low findings.
- Planner re-verified everything locally: tsc/lint green, `yarn build` with zero env green (45 static pages, catalog ●SSG, readable slugs `patches/waiting`…), zero firebase in tracked files, `firebase.json`/`.firebaserc` gone, cart key bumped to `utg-cart-v2`, `revalidate: 3600` present, no `details` in place_order 500. Runtime smoke on :3011: uk titles in view-source, `/en` with rates down shows ₴ only (the reviewer-visible `$12` matches are RSC flight-payload refs like `"error":"$12"`, verified not prices), `/fr/*` 404, unknown slug 404, favicon 200, `/api/categories` 404, «Death» tee page renders uk description + «Немає в наявності», all 13 product images shipped.
- Reviewer findings routed: (1) cart image relative path — REFUTED as issue: bot source (extracted/bot-contract-index.js) consumes only title/quantity/productUrl, image field is dead weight in the payload; (2)+(3) resolveLocale placement + error.tsx swallow → DEF-16 (step 2); (4) redirect-then-404 for `/fr` — accepted cosmetic; (5) stickers1 "unused" — intentional per Gate A amendment.
- Reviewer highlight worth keeping: step 1 incidentally fixed latent never-seeded `languageState` (en pages previously linked to /uk and formatted uk). DEF-1/DEF-8/DEF-12 CLOSED; DEF-17 new (framer-motion orphan → 4a). Merge delegated to user (squash).
- Merged as `49acce7` (−1731 net lines). **Prod rescue confirmed live** on ua-tactical-gear.com: Vercel auto-deployed master (deployment READY), planner smoke-tested production — uk titles in source, product images 200 direct + through optimizer, `/en` shows real USD (rates env live on Vercel), product pages 200. The outage (Firebase 402/504) is over; site fully functional for the first time since Storage was sealed.
- CLAUDE.md rewritten to post-step-1 architecture (static catalog, SSG pages, single API route, local assets, money model + known staleness quirk). `step-2-state-prompt.md` issued.

## 2026-07-18 — Design lane CLOSED; step-2 in flight

- Design session finished remaining items (uk tee descriptions pasted); user visually reviewed the full kit and approved. **D-4 fully RATIFIED**; the Claude Design project is the visual SSOT for 4a–4c. No further designer prompts — all surfaces incl. 404 exist; reopen point-wise only if 4b finds gaps.
- User proposed commissioning a "first page prototype" — planner corrected: prototypes already cover every page (approved minutes prior); nothing to commission. User's port-first instinct confirmed as the existing plan: 4a ports the system (shadcn init, tokens verbatim via DesignSync, primitives as owned components), 4b/4c compose ONLY those primitives — per-page styling is banned by scope fence.
- Plan refined: Tailwind 3→4 folded into 4a (shadcn init rebuilds the styling layer anyway; keeping step 3 = Next 16 + React 19 + DEF-6 only), framer-motion drop (DEF-17) assigned to 4a explicitly. Sequence stands: 2 → 3 → 4a → 4b → 4c.
- Step-2 executor running; awaiting its plan & design gate.
- D-3 RATIFIED by planner (user delegated, career lens): commit `initiatives/` + `CLAUDE.md` — public inspectable proof of disciplined AI-agent orchestration beats an unverifiable resume claim; step-7 README will frame it as a feature. Hygiene: probe junk pruned from `extracted/`, `extracted/assets/` gitignored, extracted README rewritten post-step-1. First docs commit pushed to master by planner (`bf01b27`).

## 2026-07-18 — Step-2 Gate A approved

- Executor verified all 4 cart bugs on master with line numbers and self-corrected its own grep miss (`main`/`category` dictionary blocks are used; only `categories` has 0 consumers). Standout discovery: `utg-cart-v2` persists a RAW JSON array (atom effects), not Zustand's `{state,version}` envelope → custom `PersistStorage` reading/writing the bare array, keeping byte-compat (the gate that would otherwise have silently wiped carts) and forward-compat for DEF-3.
- Design signed: two stores (cart persist / sidebar ephemeral — different persistence boundaries); `skipHydration` + post-mount rehydrate replaces the `isClient` hack; single `I18nProvider {locale, dictionary, money}` (values change together — split contexts buy nothing) fixing the staleness bug via props-reading context; cart API keyed by caller-supplied id (DEF-3-ready); `QuantityStepper` extraction with draft-string + commit-on-blur + store clamp; `Dictionary` = `typeof en` with leaves widened to `string` + `satisfies Record<Locale, Dictionary>` (uk drift = compile error, values free to differ).
- Judgment calls signed as proposed: `resolveLocale` → `src/utils/locale.ts`; `constants/recoil.ts` folded into `store/cart.ts` (deleted, not renamed); provider under `src/i18n/`; no tests (step 6). Task shape: additive infra → one atomic flip commit (half-migrated tree = two cart sources of truth) → lint.
- Planner footnotes: delete emptied `src/providers/`; DEF-16's second half (error.tsx `console.error`) must be in a task.

## 2026-07-18 — Step-2 PR #4 planner-approved

- Executor: 8 atomic commits, additive→flip structure held; its max review fixed 5 findings (qty normalization at every store entry, Array.isArray guard on persisted data, stepper blur canonicalize, checkout hydration gate, shared selectors), deferred 1 correctly (bare-slug cart id → DEF-3). Independent reviewer approved with 4 minors, verified slug global uniqueness (19/19).
- Planner re-verified: both Gate-A footnotes landed (providers/ gone, error.tsx logs); eslintrc override deleted; recoil zero everywhere; `categories` block gone from both dictionaries; build zero-env green, all pages ● SSG, bundle −23kB/route confirmed (category 103kB vs 126kB); **drift-guard proven live** (deleted `cart.total` from uk.json → tsc error naming the key; restored → green). User ran the browser gates: locale switch updates strings+prices instantly, no hydration warnings, checkout toast OK.
- Reviewer findings routed: one-way drift guard (extra uk keys pass) — accepted asymmetry, the harmful direction (missing → undefined render) is covered; slug-uniqueness constraint promoted into DEF-3's entry; stepper draft-vs-± edge accepted; sidebar.ts "use client" asymmetry justified. DEF-5, DEF-16 CLOSED.
- `step-3-upgrade-prompt.md` issued (/upgrade Next 16 + React 19; async params, next-lint retirement, caching semantics, React-19 peer sweep with bump-not-replace rule, DEF-6 browserslist). Merge of #4 delegated to user (squash).

## 2026-07-18 — Step-3 plan gate approved (+ framer-motion pulled forward)

- Research verified GO: 4 required breaking changes (async params ×6 fns, `next/dynamic ssr:false` in server component — build-hard-error invisible to tsc, fixed via thin client wrapper keeping page metadata; `useRef()` args ×3; ESLint 9 flat config extending only core-web-vitals). flowbite-react@0.10.1 accepts react@19 peer-wise; zustand@5 ready; Tailwind stays 3.
- Plan agent caught 2 research gaps, both verified: (a) framer-motion peer `react: ^18` → would warn forever post-upgrade; (b) quality call-sites are 8, not 2 → without `images.qualities: [0,75,100]` seven images silently degrade 100→75 (visual change smuggled into a platform PR). Config line approved; CartItem `quality={0}` stays as-is until DEF-11.
- O2 decided by planner: remove framer-motion NOW (option 1) — the upgrade changed the fact base (dormant → warning); deleting a grep-proven-unused dep is upgrade hygiene, not a D-9-style replacement. DEF-17 CLOSED early (was: 4a).
- Watch items for implementation: flowbite runtime on React 19 checkout form (fallback: bump within 0.x), Turbopack-default build (fallback: `next build --webpack`).
- Mid-implementation gate: Next 16's core-web-vitals bundles react-hooks v6 → 2 net-new Compiler-era rules fail 3 idiomatic pre-existing sites (green on 14). Planner: preserve baseline (disable the 2 new rules; rules-of-hooks + exhaustive-deps stay) — a platform PR holds policy constant; not-adopting new rules ≠ re-disabling ones we restored in DEF-5; compiler rules without the compiler is cart-before-horse; Zustand's canonical skipHydration idiom must not be rewritten on the sacred persistence path; 2 of 3 sites are rebuilt in 4a anyway. Adoption recorded as DEF-18.

## 2026-07-18 — Step-3 PR #5 planner-approved; browser gate R1 → user

- Executor: 6 commits (upgrade → async params → checkout client-wrapper → useRef args → qualities pin → proxy rename + flowbite namespace-import fix), review SHIP. Reviewer independently confirmed: all async-params surfaces covered, `flowbite-react/tailwind` has no default export (old import would crash under Turbopack — real catch), MutableRefObject cast removal honest, proxy redirect smoke-tested.
- Planner re-verified on the branch: versions 16.2.10/19.2.7, framer-motion zero in package.json+lockfile, `src/proxy.ts` (middleware gone), flat config disables exactly the 2 DEF-18 rules with core-web-vitals+exhaustive-deps active, `--frozen-lockfile` clean, tsc/eslint/build zero-env green, route table identical (all ● SSG; Turbopack compiles 6s vs ~20 on webpack), prod-server smoke: all pages 200, `/fr/x` 404, checkout serves spinner shell, catalog titles in source.
- Deviation vs plan gate, accepted: `images.qualities` landed as `[75,100]` not `[0,75,100]` — Next 16 rejects quality<1 entirely, so CartItem `quality={0}` → 75 (forced, correct). DEF-11 CLOSED as side effect (4b keeps sizes/priority hygiene). New DEF-19: checkout `ssr:false` wrapper is redundant post-`useCartHydrated` — remove when flowbite dies (4a/4c). Reviewer's tsconfig note (`jsx: react-jsx`) — watch whether `next dev` rewrites it back (parasitic-diff risk, low).
- Remaining before merge: user's browser gate R1 — flowbite checkout form interactively under React 19 (only surface not verifiable headless). Fallback ready: flowbite-react 0.12.17.
- R1 passed by user; PR #5 squash-merged (`f3f79bf`). Step 3 CLOSED — the platform stack (Next 16 / React 19 / ESLint 9 / Turbopack) is final; 4a unblocked.
- Post-merge hygiene by planner: user's local `yarn format` run surfaced that PR-5 code wasn't fully prettier-clean (line-width wraps) and that `next dev` had rewritten tsconfig arrays (the reviewer's predicted parasitic diff, in prettier↔next-dev form). Resolution: `.prettierignore` for `extracted/` (documentary sources stay verbatim), repo-wide format commit, CLAUDE.md rewritten to post-step-2+3 reality (Zustand/i18n context/proxy.ts/ESLint 9/DEF-18 warning for agents), `.upgrade/` gitignored.
- Prod smoke on the Next-16 deploy: home/product/checkout/images all 200, uk product description renders. Platform migration invisible to users — as it should be.

## 2026-07-18 — Design exported to repo; D-10 sealed-module; 4a prompt issued

- Design system exported from the Claude Design project into `design-export/` (committed, prettier-ignored, read-only for executors): all 6 token CSS files verbatim (colors with shadcn aliases, fluid type scale, spacing/borders/motion, element base, **components.css — the full per-primitive visual spec**, fonts as reference), `system-readme.md` (visual language + content rules), `component-specs.md` (Header/Footer/SectionBand/CartDrawer/Toast intents). The 19 per-component prompt.md files were deliberately NOT pulled — components.css carries the complete visual truth; API design belongs to the implementation.
- **D-10 RATIFIED** (user directive: «запечатана и обтянута колючей проволокой»): DS = sealed module in `src/design-system/` — single barrel API (deep imports lint-banned), raw colors only in the theme (Tailwind default palette wiped → non-token color classes don't compile), font sizes only inside (app text goes through variant-based `Typography`), `Container` with `maxWidth` owns page width. Enforcement = theme wipe + ESLint restricted imports + grep gates in every review from 4a on.
- `step-4a-design-system-prompt.md` rewritten around D-10: sealed module + Tailwind 4/shadcn + next/font + full primitive set (+Typography/Container) + chrome (Header/Footer/CartDrawer-Sheet) + retirement of flowbite/notyf/body-scroll-lock + mechanical class sweeps (legacy palette/text utilities die with the wiped theme) + DEF-19. Page re-composition stays fenced for 4b/4c.

## 2026-07-17 — Review, roadmap, initiative bootstrap, step 0 issued

- Planner reviewed the full codebase (48 files) + baseline checks: tsc green, lint green, `yarn build` **fails without env** (module-level Firebase init).
- Key findings: place_order route serializes the `Response` object (checkout always "succeeds", cart cleared on failed orders); artificial 1s `setTimeout` on all 4 data pages; all-client-side architecture (zero SEO, fetch waterfall, 4 route groups as metadata workaround); Recoil abandoned/React-19-incompatible; cart bugs (state mutation, index keys, quantity desync); fonts from Wix CDN; dead code (`useInitializeCart.ts`, `exchangeRatesState`, `dotenv`); no tests/CI; boilerplate README.
- Roadmap ratified by user as 10 steps, each one executor run in a separate Opus 4.8 tab (D-1, D-2, D-6). Initiative structure bootstrapped (mirrors film-dna/the-discipline-program convention).
- Step 0 (quick wins) prompt written: `step-0-quick-wins-feature-prompt.md`. Env-tolerant build folded into step 0 (was step 1) so executor PR verification is unblocked without secrets.
- Open: D-3 (git-track planner artifacts?), D-4 (visual direction), D-5 (deploy/live-shop/env facts).

## 2026-07-18 — Step-4a Gate A approved (6 deviations blessed)

- All six executor deviations approved as improvements, not compromises: (1) chrome lives in app-land composing DS primitives — DS stays app-agnostic (dependency direction; the seal is about styles, not app-state ownership); (2) stylesheet wired via CSS `@import` so ZERO JS deep-imports exist — the sanctioned ESLint exception became unnecessary (stricter than spec); (3) type scale kept out of the `--text-*` theme namespace so Tailwind cannot generate a friendly `text-hero` bypass around `Typography` (closes a hole the planner's spec missed); (4) LanguageSwitcher stays a native `<select>` styled to DS look (routing behavior + a11y over Radix ceremony on a 2-option control); (5) category-card availability marks map to `Badge` in/out per the kit's card spec, not Button (design fidelity); (6) one atomic PR — the palette wipe is a cutover: half-migrated pages would render unstyled; same topology as step-2's flip.
- Footer mission copy "blocker" dissolved: planner supplied the ratified kit strings verbatim from its earlier DesignSync read of `ui_kits/storefront/data.js`. DEF-14 partially consumed (mission line); remaining kit copy lands in 4b/4c.
- Process lesson recorded: this entry originally lived only as an uncommitted working-tree edit; the executor's pre-PR cleanup (`git checkout -- initiatives/`) reverted it. Promotion to the initiative now always ends in an immediate commit.

## 2026-07-18 — Step-4a PR #6 planner-approved; browser walkthrough → user

- Executor delivered the sealed DS in 9 substantive commits + review fixes (its max review, 24 agents, found 10 real sweep-regressions — dead card clicks, blue-underlined heading links under new base `a{}`, footer hydration mismatch, drawer not closing on Back/Forward — all fixed in-branch). Bonus catch beyond spec: element defaults in `@layer base` so token utilities win the cascade.
- Independent reviewer: merge, 5 minors. Planner re-verified everything: frozen-lockfile install, tsc/lint zero, all four seal greps ZERO, **seal-scream re-proven first-hand** (planner's own probe → exactly 4 ESLint errors with instructive messages), zero-env build green, route table intact (44 pages, checkout ● SSG → DEF-19 CLOSED), token fidelity verified in built CSS (all five brand hexes as `--color-*` vars; default palette and `--text-*` scale absent; `.type-*` classes outside the Tailwind namespace; fonts local next/font woff2, zero external URLs). Verification footnote: under Turbopack the built CSS lives in `.next/static/chunks/`, not `.next/static/css/` — planner's first grep hit the wrong path and produced a false MISSING.
- Reviewer minors routed to DEF-20 (NavOverlay aria-describedby, Button asChild+loading latent trap, Footer "use client", LanguageSwitcher unanchored replace) — all four land in 4b which touches those exact files; round-tripping the executor for dev-only/latent one-liners wasn't worth the cycle. DEF-18 updated: only the canonical Zustand skipHydration site remains (2 of 3 died with 4a, as predicted).
- Remaining before merge: user's interactive browser walkthrough (drawer focus/scroll/esc, stepper, checkout fail-path toasts, both locales) — render-ability of all 44 pages proven by the build; interaction is the unverifiable-headless residue.

## 2026-07-18 — 4a browser walkthrough: 6 defects triaged, fix round issued in-branch

- User walkthrough verdict: much better, plus 6 defects. Triage against the 4a fence: **in scope, fix now** — (1) QuantityStepper stretches full-width (spec says inline-flex hug), (4) header locale switcher is a select but the kit shows a two-text-button toggle with flag-yellow active underline — this SUPERSEDES Gate-A deviation #4, which the planner blessed without re-checking the kit (planner error, corrected), (5) footer doesn't match the kit's two-row band layout, (6) cart-line remove must confirm via the Dialog primitive — the kit's Dialog demo IS this exact interaction (remove-item title, ghost cancel / destructive remove), built in 4a but left unwired. **Out of scope → 4b** — (2) breadcrumbs/back-link (screens composition), (3) product cards (ProductCard primitive deliberately deferred; transitional look is by design).
- Kit references for all fixable items exported verbatim to `design-export/chrome-reference.md` (header switcher, footer structure, dialog usage, stepper hug rule); en dialog strings planner-approved (kit shows uk only). Fix lands on the same open PR #6 branch — merge stays atomic.
- DesignSync footnote: the design project 404'd until the user re-ran /design-login (multi-account switch); project id unchanged.

## 2026-07-18 — Handoff prepared: planner session rotation planned

- User scheduled a planner-context rotation: 4a fix round → merge #6 → 4b run → merge → handoff to a fresh planner session (this session is at ~47% context).
- `planner-handoff-prompt.md` written and committed — deliberately state-agnostic (points at the resume protocol instead of snapshotting the board, so it cannot go stale). It carries the only knowledge that lives outside the repo: the operating loop as practiced, the promotion-then-commit-immediately rule, independent-verification discipline, Vercel/DesignSync/bot coordinates, local hook/Turbopack/tsconfig quirks, and the collaboration contract with the user.

## 2026-07-18 — 4a fix round approved and merged; D-10 gains control-ownership layer

- Fix round (272c051) planner-verified: commit clean (15 src files + eslint config), all SIX seal greps zero (4 original + raw-elements + dialog-internals), raw-element rule proven screaming by planner probe (doubled by Next's own no-html-link-for-pages), stepper w-fit at the primitive root (correct diagnosis: inline-flex flex-item stretched by parent align-items), TextTab/IconLink own all visual state, Dialog rebuilt as intent API (title/children/actions, size panel|full — NavOverlay collapsed into Dialog size=full, stricter than asked) + ConfirmDialog, remove\_\* keys in both locales. User walkthrough passed. PR #6 squash-merged (`2e83503`).
- D-10 addendum ratified mid-round from two user directives: (a) styled interactive elements (buttons, text-like links/tabs) are DS-owned — app-land raw <button>/<a> is an ESLint error; layout containers stay app-land; (b) composite patterns are exported as closed intent-API compositions (Dialog, ConfirmDialog), building blocks stay internal — "the DS exports intents, not constructors."
- Executor judgment calls accepted: flat remove\_\* dictionary keys (2-level Dictionary type kept), stale-.next diagnosis for the phantom text-white error (this shared tree branch-hops daily).
- Git hygiene note: planner docs (journal/handoff-prompt) rode the working tree through the user's PR-branch checkout; stash → master → pop → conflict-resolve → this commit. The shared-tree dance is now documented in the handoff prompt.

## 2026-07-18 — 4b prompt issued; kit screens exported; carousel discovery

- Kit screen compositions (Home/Category/Product/Reports+About+404) and the full copy table exported verbatim to `design-export/kit-screens-reference.md` (+ current `data.js` re-pulled: nf\* strings exist since the 404 build; kit also fixes the awkward legacy en about1 "under-boss's" → "the unit we support").
- Discovery: the ratified kit replaced the reports CAROUSEL with a numbered figure grid — swiper dies in 4b without a replacement dependency (plan.md updated; better than the original "swiper → DS Carousel" plan).
- `step-4b-screens-prompt.md` issued: new DS components (ProductCard, category tile, size-selector — the kit prototypes chips as raw buttons, which our own seal now forbids in app-land), screens per reference with NO loading states (SSG vs the kit's skeleton theater), DEF-3 composite cart id with size encoded in the line TITLE (payload keys sacred — no new fields; additive compat with old persisted carts), DEF-14 copy, DEF-20 sweep, image hygiene. Checkout untouched (4c).
- This is the last run before the planner handoff (per user's plan: 4b merge → rotation).

## 2026-07-21 — Pre-4b: overlay defect confirmed, prompt amended; D2 review gap surfaced

- User (back after two days) reported the burger overlay misaligned vs design. Verified both sides: kit Header.jsx overlay is LEFT-aligned (column at 10vh, large display links `clamp(2.5rem,8vw,4rem)` with baseline mono numbered prefixes, Instagram pinned bottom via auto margin); our NavOverlay centers everything (`items-center justify-center`) at h2 scale. Real fidelity defect ×2 (alignment + scale/structure). NavOverlay spec appended to `chrome-reference.md`; 4b prompt amended (scope item 6 + gate line).
- User also asserted screen prototypes were never designed ("дизайн сделан только для дизайн-системы"). Planner verified before folding: prototypes of ALL surfaces exist in the design project (`ui_kits/storefront/` — clickable, both locales; planner read and exported them verbatim to `kit-screens-reference.md`; the punch-list round edited screen files data.js/Reports.jsx/App.jsx; the 404 was built there). The expected "D1 → review → D2" sequence collapsed into one delivery because the designer consumed both briefs from the mounted folder at setup. HOWEVER the user's recollection implies their visual review may have covered the system sheet only — the D2 gate's "every surface approved" may not have genuinely had user eyes on screens. Resolution: user clicks through `ui_kits/storefront` in the Design UI in parallel with the executor's plan phase (non-blocking — changes fold in at the plan gate); D2 status downgraded pending that pass.
- Handoff plan unchanged: 4b run → merge → planner rotation (start prompt delivered in chat = handoff file + current-position snapshot).

## 2026-07-21 — D-11: kit screens are demos; per-page design→implement loop; 4b rescoped

- User resolved the D2 question with content: the storefront kit screens were made as design-system showcase demos — not ratified page designs; real page prototypes don't exist yet. Product domain = user's; planner folded and restructured. D-11 RATIFIED: DS is FROZEN (changes only via Claude Design + explicit ratification); pages get a per-page loop — planner brief → Claude Design → user+planner ratify → export → one `/feature (small)` implementation run per page.
- Plan restructured: old 4b (implement-all-screens) SUPERSEDED (prompt file banner-marked, never ran; its DEF-3 cart-identity design and image-hygiene gates remain input for per-page prompts); new 4b = DS alignment in app (NavOverlay fidelity + DEF-20, `/feature small`, prompt issued); D3 = per-page design track (Home brief issued — fresh composition invited, DS additions must be proposed explicitly for ratification); 4c–4g = per-page implementation steps. DEF-3 → product-page step; DEF-14 → per-page; DEF-20 → 4b. Stale statuses fixed along the way (plan.md rows 0–4a marked done; decisions index D-4 OPEN→RATIFIED drift corrected).
- Handoff timing per the standing plan ("after the next run's merge"): after the 4b alignment PR merges; the new planner drives the per-page loop.
- Brief amendments per user: (a) ONE thoroughly-worked composition, no variant forks; (b) Claude Design DOES get code access after all — the standing rule flips to "user attaches a fresh Desktop snapshot per brief" (planner refreshed `Desktop/utg-2.0`, 18M, incl. the sealed DS — the designer now sees the implemented system as ground truth, not just its own kit).

## 2026-07-21 — D3.1 Home ratified and exported; 4c prompt staged behind 4b

- Designer (fresh session with the attached code snapshot; spawned its own project `01558ea9-…` mounting the DS read-only) delivered ONE worked Home composition; user approved visually. Planner fidelity review: 100% token vocabulary (type-hero wordmark, space/gutter/container tokens, utg-card/band/rule--heavy), zero raw colors/sizes, no unratified DS additions; composition is genuinely fresh (stamp+mission under a heavy rule, numbered tiles 01–03, CTA as in-page anchor to the merch band per the ratified design) and carries a DEF-9-ready hero-photo slot (skull swaps out when a photo arrives) — DEF-9 disposition updated.
- Exported verbatim to `design-export/screens/home/` (Home.dc.html + home-reference.md with structure, verbatim copy table incl. the split mission line + new stamp/counter keys, and D-10 mapping notes: labeled icon-link + numbered CategoryTile enter the DS in 4c, tile reused by 4d).
- `step-4c-home-prompt.md` issued with an explicit precondition: carry ONLY after the 4b merge (both steps touch the DS; serial by design). D3 loop note: per-brief designer sessions spawn their own projects — planner reads whatever URL the user brings via DesignSync and exports into the repo.

## 2026-07-21 — 4b plan gate approved (one deviation returned with a cheaper fix)

- Executor recon verified DEF-20 against master: #1 (aria-describedby) already closed by the 4a fix round — not-applicable with proof; #2 has 3 asChild call sites but zero passing loading → type-level ban (discriminated union, asChild:true ⇒ loading?:never) breaks nothing; overlay link size genuinely needs a new DS type step → `nav` Typography variant (clamp(2.5rem,8vw,4rem)/1.15) inside the DS token pattern (plain CSS vars, still outside the Tailwind @theme namespace — the 4a anti-bypass hole stays closed).
- Deviation verdicts: (1) numbered prefix stays `caption` (0.75rem) vs kit 0.875 — APPROVED, with a stronger rationale than the executor's: the ratified Home design's numbered tiles also use caption for their 01–03, so caption-everywhere beats kit-verbatim one-off consistency-wise; no near-duplicate variant. (2) overlay wordmark NOT deferred — the Dialog composite already renders a `title` in its band row; passing `title="UTG"` instead of `"Menu"` reproduces the kit's top row (h3 display caps ≈ the kit's 1.375rem wordmark) as a one-line prop change, no composite surgery; a11y note: keep an sr-accessible menu label if trivial. (3) close-row height stays the sealed composite's concern — APPROVED as recommended.
- Mod #2 bounced back with evidence: the planner's premise was wrong — dialog.tsx renders the title VISIBLY only in the panel branch; in size="full" it is sr-only (planner had grepped that title renders, not in which branch). Executor verified before acting, refused to ship a dead prop, surfaced A/B. RULING: A — the full-size branch gets a visible band-row title mirroring the panel branch (Typography h3, band-foreground, ~6 lines; sole consumer is NavOverlay; a full-screen dialog showing its header title is conventional). The "do not touch the composite" fence is lifted for exactly this change — it was attached to the wrong premise; this is ratified DS evolution, not a NavOverlay hack. Accessible name becomes "UTG" — accepted; no extra aria prop surface for a hypothetical.
- Executor honesty flag accepted: swiper remains in ReportsScreen — pre-existing, exempt from 4b's gates (the retired-deps grep is flowbite/notyf/body-scroll-lock as defined in 4a); swiper dies in the reports-page step (4f) per D-9/plan.

## 2026-07-21 — 4b PR #7 planner-verified; one-class fix round before merge

- Independent verification on the branch: 8 atomic commits, planner-docs commit confirmed stripped, zero planner files; tsc/lint/prettier/zero-env build green, route table all-SSG; seal greps zero; **the asChild/loading type ban proven live** (planner probe `<Button asChild loading>` → TS2322 naming the incompatible prop).
- Reviewer findings ruled: [low] overlay lacks any overflow handling — planner grep confirmed (no overflow-\* in NavOverlay/dialog); on short landscape viewports the menu clips unreachably; real functional defect, one utility class, executor tab warm → fix-before-merge round. [info] `--text-nav` narrative nuance accepted (same clamp bounds as h1, steeper mid-ramp + 1.15 leading justify the token; wording only). [info] accessible name "UTG" — already ruled, stands.

## 2026-07-21 — 4b merged; planner handoff issued

- Overflow fix round landed (overflow-y-auto on the overlay children column, verified in merged master) and PR #7 squash-merged (`8d9a4ba`). DEF-20 CLOSED (all four: not-applicable-with-evidence / type-ban proven by planner probe / Footer server-ified / switcher anchored). CLAUDE.md Typography variant list gains `nav`.
- This closes the last run of this planner session per the standing plan. Handoff prompt refreshed (D-11 per-brief-project pattern replaces the stale old-4c DesignSync note) and issued in chat with the position snapshot. Session totals for the trail: PRs #2–#7 merged (quick wins → rescue/de-Firebase → Zustand → Next 16/React 19 → sealed DS + fix round → DS alignment), prod rescued and redesign foundation complete; the fresh planner takes over at 4c (Home implementation, prompt staged) and drives the D-11 per-page loop.

## 2026-07-21 — Planner session 2 resumes; deferred board trued up

- Fresh planner session ran the resume protocol; board cross-checked against `git log origin/master` (`796a4d9`, clean tree, no open PRs) — position matches the handoff snapshot exactly. Zero-env `yarn build` on master green in 7.3s, route table all-SSG.
- Deferred hygiene: DEF-6 and DEF-7 were still SCHEDULED but verified done — DEF-6 resolved by the step-3/4a lockfile refresh (caniuse-lite 1.0.30001806, no browserslist warning in the zero-env build), DEF-7 shipped ahead of its step-5 slot (local `src/app/favicon.ico`, all assets in `public/`, zero `firebasestorage` refs repo-wide). Both CLOSED with evidence; DEF-18 added to state.md's live carry-forwards line where it was omitted.
- Next action unchanged: user carries `step-4c-home-prompt.md` to a fresh executor tab; planner reviews the plan gate, then the PR, per the loop.

## 2026-07-21 — 4c PR #8 planner-verified; three-item fix round ordered before merge

- Independent verification on `feat/home-recompose` (4 commits, 6-file diff, zero planner artifacts): tsc/lint green; zero-env build green, route table unchanged all-SSG; all six seal greps zero; the IconLink accessible-name union proven by planner probe (no label + no aria-label → TS2322); prod-server smoke green on both locales (wordmark, stamp, mission, counter, `#merch` anchor + CTA href, tile numbers, all three category links). The `prettier --check` failure was planner-owned (session-2 deferred.md edit desynced table column widths; fixed by this commit's `yarn format`) — all six PR files were formatted.
- Amendment-2 trail (Instagram 18px): `IconSize` is a frozen `20 | 24` union; the executor first hit 18 via a CSS override, then normalized to DS-native 20 (47553c2). RATIFIED at 20 — the reference's 18px is mock-raw vocabulary and the sealed union is the translation target (the same logic that ratified the D-11 fidelity bar). The PR-body risk note still describes the CSS override — stale, fixed in the round.
- Review findings ruled: (1) NavOverlay hand-rolls the just-sealed labeled treatment → fix now, swap to the `label` prop; the sealed gap-2/min-h-12 treatment WINS over the kit's 10px overlay gap (one treatment per D-10; the 48px target is an a11y gain; the 4c scope fence is extended by exactly NavOverlay.tsx, planner-ratified). (2) Stale PR-body note → fix in round. (3) CategoryTile's hidden `.group` contract → fix now: the hover trigger moves to the tile root (self-contained), the `group` class drops from HomeScreen's NavLink — kills the silent 4d coupling. Nits: `label.trim()` guard folded in (the residual `label=""` type corner accepted as theoretical); the spacing token/scale mix accepted (values correct; spacing utilities are not sealed); quality 100→90 REJECTED with config evidence — `images.qualities: [75,100]` (step 3) makes 90 a Next 16 error, the suggestion would have broken the page.
- Browser gate stays with the user post-round (CTA scroll, tile nav, Instagram new-tab, 375/1200 wrap, the overlay row after the swap, console clean), then squash-merge.

## 2026-07-21 — 4c merged and live in prod; D3.2 catalog/category brief issued

- Fix round read line-by-line in the shared tree (9+/9− across 4 files: NavOverlay adopts the sealed `label` treatment; CategoryTile self-contains its hover via a named `group/tile` — cleaner than the planner's own sketch, immune to future ancestor groups; `label.trim()` guard) and PR #8 squash-merged by the user (`dec9a78`). Merged master re-verified: tsc/lint/zero-env build green, all three fix-round changes present.
- Prod verified end-to-end: Vercel deployment on `dec9a78` READY (production target), live-domain smoke green (apex 308→www; stamp, counter, and the `#merch` anchor in the served HTML of both locales). Step 4c CLOSED — the shop runs the ratified Home.
- Bookkeeping: CLAUDE.md gains `CategoryTile` + the IconLink `label` treatment and drops two stale claims (the DEF-19 checkout wrapper died in 4a; fonts have been self-hosted since 4a); plan.md 4b/4c stale statuses fixed; DEF-13 SCHEDULED to the 4f cart/checkout window; DEF-14 notes the Home keys landed.
- D3.2 brief issued (`design-4-catalog-category-prompt.md`): two surfaces in one step — catalog index `/category` (must earn its existence next to Home's tile grid; CategoryTile reuse mandated, extensions proposed explicitly) + category product grid (three real instances as design-load: 5-product patches, 1-product stickers, all-out-of-stock tshirts; longest set title in the prototype). Verbatim copy inline incl. existing `category.order`/`category.out`; ProductCard expected as an explicitly proposed DS addition with a full state set; out-of-stock treatment per ratified system law; no loading states. Desktop snapshot refresh follows this commit.

## 2026-07-22 — D3 mechanics amended: one cumulative design dialog

- User directive with rationale: page briefs now land in the SAME Claude Design dialog that produced Home — not fresh per-brief sessions — so the design project accumulates into a full clickable prototype of the site on mocked data. Planner endorsed: cross-page consistency and click-through navigation come free, and the finished prototype doubles as a step-7 presentation artifact. Supersedes the "per-brief designer sessions spawn their own projects" note from the D3.1 entry.
- D3.2 brief amended in place: same-dialog header; the freshly attached snapshot explicitly SUPERSEDES the previously attached one (guards against two codebases fighting for ground truth in one context); new deliverable — wire the navigation so Home → catalog index → the three category instances click through.
- Fallback noted: if the cumulative dialog degrades over the remaining briefs (contradicting the frozen DS, losing composition quality), spawn a fresh session mounting the same project — nothing is lost.

## 2026-07-22 — D3.2 fidelity-reviewed and ratified; exported; 4d prompt issued

- User approved both surfaces in the Design UI; planner review on the same cumulative project (`01558ea9-…`): token fidelity 100% — every inline value is a token or a D1-ratified card-language pixel; the entire ProductCard vocabulary (`utg-card-media/body/title`, `utg-badge--out`, `utg-btn--sm`, `utg-card--out`, hover zoom 1.03) is the D1 kit's own components.css, composed not invented; the kit's out-ink hex #5D554C maps to our `--color-ink-muted` token. Data module byte-faithful to the sacred catalog (all 12 products) with correct uk plural rules. Honesty checks pass: tshirts announces «6 товарів · Немає в наявності» in the band meta, out cards stay navigable with no order affordance, zero flag-yellow on both pages (deliberate accent budget for navigation surfaces).
- Home in the project verified against the ratified D3.1 export by real diff: 38 changed lines, every one navigation/hash/gating plumbing, zero visual or copy change — export refreshed to the wired version (export = project truth), `home-reference.md` untouched, no re-ratification needed.
- Ratified DS changes for 4d: `ProductCard` (full state set; NO in-stock badge on grid cards — yellow rarity, the in-badge belongs to the product page; order action as a presentational span inside the single card link, 40px height acceptable because the whole card is the target), `CategoryTile` meta extension (optional additive row — Home renders byte-identically without it), `SectionBand` extension at intent level (baseline title row h1|h2 + right meta caption + linkable kicker; exact React API = the expected 4d plan-gate question; internal nav stays app-land). `Price.muted` already shipped — no change. Copy hygiene call: the en in-stock key lands as "In Stock" (sibling-casing consistency; the prototype's "In stock" renders identically under the uppercase transform).
- Exported verbatim: `screens/catalog/` + `screens/category/` (+ `screens/data/catalog.js` derivation module with the uk plural algorithm) with distilled references; `step-4d-catalog-category-prompt.md` issued (`/feature small`). New DEF-21: Home's inline merch band vs the extended SectionBand — retrofit opportunistically, zero visual delta expected.

## 2026-07-22 — 4d plan gate approved with two API refinements; accessor scope granted

- Executor's SectionBand proposal (meta + kicker ReactNode + kickerAsChild Slot) accepted with two contract refinements, both house patterns: (a) type-level kicker union — `kicker?: string` (Typography caption path) XOR `kicker: ReactElement` requiring `kickerAsChild: true` (Slot path); an element without the slot flag must be a compile error, closing the styled-node drift channel the same way the IconLink label union and the 4b `asChild ⇒ loading?: never` ban did; (b) the container wrapper (`mx-auto max-w-(--container)`) is UNCONDITIONAL, not meta-gated — layout must not switch on an unrelated content prop, and 4g's reports band (kicker + title, possibly no meta) would otherwise need a second implicit mode later. Byte-identity for surviving consumers planner-verified with evidence: Reports/About/Checkout all render `center`, and centered text in a centered container is positionally identical; if the executor's own verification contradicts this, fall back to meta-gating with the evidence in the PR.
- Scope flag ruled: the catalog accessor derivation is APPROVED and the 4d fence formally extends by `src/data/catalog.types.ts` + `catalog.ts` (accessor-only). Constraints: extend the existing `CategorySummary` (numeric `count` + boolean availability) rather than adding a parallel accessor; the data layer returns FACTS — localized strings compose screen-side (dictionary keys + the `Intl.PluralRules` util); data literals stay untouched; HomeScreen's consumption of `CategorySummary` is additive-safe.
- Rounding rulings: Button gains a `sm` size only if it lacks one (utg-btn--sm values verbatim — part of the ratified ProductCard treatment); the order action renders through `Button asChild` onto a span (no nested interactives — approved as proposed).

## 2026-07-22 — 4d PR #9 planner-verified; two-line fix round before merge

- Independent verification on `feat/catalog-category-pages` (10 atomic commits, 11 files — exactly the granted fence, zero planner artifacts): tsc/lint/prettier/zero-env build green, route table all-SSG, all six seal greps zero; kicker discriminated union implemented per Refinement 1; `--hit: 44px` token real. SSR smoke green on all instances: derived counter + per-category counts/availability in `/uk/category`, tshirts announces «6 товарів · Немає в наявності» with zero order labels, stickers renders one card, patches carries the full set title, en casing "In Stock" landed per the ruling.
- Reviewer's 🔴 CONFIRMED by planner arithmetic against the code: SectionBand puts the gutter OUTSIDE the container box (outer `px-(--gutter)`, inner `max-w`), Container puts it INSIDE — at `--gutter: 56px` desktop the band content sits up to 56px left of the grid for V>1200 (exact at V≥1312; V=1440: band 120 vs grid 176). The test plan's "1200px+" checked the one width where they coincide. Fix-now: inner div adopts Container geometry (`mx-auto w-full max-w-(--container) px-(--gutter)`), outer keeps full-width bg and drops `px` — centered survivors unaffected. This also unblocks DEF-21 cleanly (Home's inline band uses Container inside the section, i.e. the target geometry).
- Second fix-now (one word): CategoryTile media `bg-paper` → `bg-white` — the kit's `.utg-card-media` is `--white`; 4c shipped paper silently, ProductCard correctly shipped white; zero visual delta (opaque object-cover photos), kills the drift question.
- Accepted as-is: en dead `few` branch (closed uk|en union, "many = catch-all" documented — revisit only if a locale is ever added); pseudo-button voice-control nuance (inherent to the ratified whole-card-link pattern). Held findings ×3 — planner concurs with executor and reviewer on all (kicker union is the planner's own refinement; re-derive equivalence proven; spec-pinned title utility inside the seal).
- Executor deviation flags ×2 APPROVED: tile accessible-name = name+meta without the decorative «01» (better than the reference's letter; label-in-name holds — the name leads); catalog counter derived (explicitly granted latitude, displayed result matches).
- Browser gate for the user must include the ≥1440px band/grid alignment check on both pages — the exact class of the caught regression.

## 2026-07-22 — 4d merged and live; shared-tree incident logged; D3.3 product brief issued

- Fix-round delta read line-by-line before the word (both edits byte-exact to the ruling: SectionBand adopts Container's box model, tile media on white); executor's deterministic SSG-HTML verification of the geometry accepted (band inner and grid Container now carry the identical class set); user's wide-viewport browser pass green; PR #9 squash-merged (`2d051b3`). Merged master re-verified: tsc/lint/zero-env build green, both fixes present. Vercel prod READY on `2d051b3`; live-domain smoke green (derived counts on `/uk/category`, tshirts out-meta, set title + en parity on patches). Step 4d CLOSED.
- Process incident logged (planner-owned): after the PR-9 verification the planner left the shared tree on master for the verdict docs commit, and the executor's fix round started against pre-4d file versions — the section-band edit failed loudly, the tile edit landed as a stray modification on master. Recovered cleanly: stray edit dropped, tree returned to the branch, round re-run. Rule promoted to the handoff canon (second corollary): while an executor round is OPEN, the tree lives on the executor's branch; planner master commits happen only between rounds, with an immediate return.
- Bookkeeping: CLAUDE.md composite list gains `ProductCard` + `SectionBand` intent + the tile's optional meta; plan 4d ✅; DEF-21 notes the box-model unification (retrofit now mechanical); DEF-14 marks catalog/category strings landed, product strings assigned to 4e; state.md decisions line trued to D-1…D-11.
- D3.3 brief issued (`design-5-product-prompt.md`, same cumulative dialog): the product page with three real content shapes as design-load — out-of-stock t-shirt (description + sizes-as-badges + outMsg + Instagram outline CTA), minimal in-stock patch (no description, no sizes — the page must not look empty), one-product sticker; the kit Product spec offered as a legitimate sketch; size-selector chips called out as the expected explicitly-proposed DS addition (the kit prototyped raw buttons — our seal forbids that in app-land); yellow budget stated: add-to-cart is THE accent moment, and the in-stock badge may finally appear here per the D3.2 departure note. Cart mechanics (DEF-3 composite identity) stay implementation-side for 4e.

## 2026-07-23 — D3.3 product fidelity-reviewed and ratified; exported; 4e prompt issued

- User approved in the Design UI; planner review on the cumulative project: token vocabulary holds (one `background:#fff` on the image frame is medium sloppiness mapping to the ratified `--white` — zero new colors or styles); the composition is fresh where it earns it (product title as the band `h1` with the category-name back-link — the shipped band language, not the kit's right-column title); the in-stock badge debuts exactly where the D3.2 departure note reserved it (`utg-badge--in` = accent bg, maps to our shipped `Badge variant="in"`); accent-budget reading ratified: one CTA (add-to-cart), the badge is a status marker, not a CTA.
- Data module extended faithfully: t-shirt descriptions byte-verbatim vs the sacred catalog (both slogans, uk+en), sizes M/L/XL/2XL only on tees, new dict strings match the kit table and existing keys verbatim; `productView`/`productHref` wire category cards → the product screen. Deltas on the other screens verified: Category = wiring-only (card hrefs через `productHref`), Catalog/Home = a React-wait bundle loader, spec-irrelevant — exports refreshed anyway for the export=project-truth invariant.
- All four content shapes ratified, incl. the data-unreachable sized+in-stock via the prototype's `demoInStock` review toggle; the out shape shows badges-not-chips, `outMsg`, an outline Instagram CTA, and nothing dead.
- `SizeSelector` ratified as the step's DS addition (chips 56/44px, 2px ink, mono 0.9375rem caps, `aria-pressed`, toggle-deselect, flag-blue focus ring; out sizes are Badges, not a chip state). Implementation notes pinned in the reference: the product image is the LCP (`priority`, not the prototype's `lazy`); kit `Price big` → our `size="big"`.
- Planner pre-rulings recorded in the 4e prompt: sized products REQUIRE a selected size (inline hint near the chips; planner-authored strings uk «Оберіть розмір» / en "Choose a size" pending user approval at the plan gate — new UI copy the kit never authored); the composed line title `{title} · {size}` carries the size through the SACRED payload with zero key changes; composite cart id slug+size with additive compat for persisted `utg-cart-v2` carts (no storage-key bump). `step-4e-product-prompt.md` issued (`/feature` — the loop's heaviest step: page + DS addition + cart identity).

## 2026-07-23 — 4e plan gate approved; three rulings incl. an OQ-4 override

- Executor's gate artifacts sandbox-proven (planned components run through real tsc+eslint in a throwaway dir, exit 0, cleaned up) — three of eight gate questions closed with evidence, the review bar this initiative wants. Approved: the controlled `SizeSelector` API (no `"use client"`, TextTab precedent, ring inherited from base.css), cart identity Design A (`slug::SIZE` id with proven collision-safety, `Title · SIZE` composed title, frozen `ICartItem` making a seventh payload key a compile error, zero-diff drawer/checkout), vacuous-but-verified backward compat (no storage-key bump).
- OQ-1 RATIFIED as (c): `text-balance` lands INSIDE SectionBand's title node unconditionally — a text-rendering refinement in the D-10-addendum spirit (the composite owns its typography), a strict improvement for every consumer, no-op on single-word titles; the app-land `[&_h1]` deep-reach correctly rejected. The 4e fence gains `section-band.tsx` for exactly this class.
- OQ-3 ruled: the required-size hint renders `text-destructive` per the field.tsx validation-error precedent — one error voice across the product.
- OQ-4 OVERRIDDEN (executor defaulted to `bg-[var(--color-white)]`): the arbitrary-value var() escape would seed a general seal-bypass precedent. The principled fix: narrow the ESLint banned-palette regex to exempt exactly `white` — the Tailwind default palette is WIPED (D-10 layer 1), so app-land `bg-white` can only resolve to OUR `--color-white` token; the ban was a name-collision false positive, and every other token already has a first-class utility. Fence gains `eslint.config.mjs` for exactly this narrowing; PR must prove bg-white compiles AND another palette word still errors.
- Prompt corrections accepted with thanks: the real key is `category.inStock` (the prompt's `in_stock` was wrong), and the orphan-keys clause is a verified no-op (all four candidates keep readers). Hint copy approval remains the user's checkbox at this gate.

## 2026-07-25 — 4e PR #10 planner-verified; no fix round needed; browser gate ordered

- Independent verification on `feat/product-page-size-selector` (7 atomic commits, 10 files — exactly the granted fence incl. the two gate grants `eslint.config.mjs` + `section-band.tsx`; CheckoutScreen absent from the diff = Design A's zero-diff claim held structurally; zero planner artifacts): tsc/lint/prettier/zero-env build green, 44/44 SSG; the white-exemption probed live in BOTH directions (`bg-white` passes; `bg-zinc-900`, `text-white`, `border-white` all still error — the hole is exactly one class, negative lookahead correct); all three ruling diffs read line-by-line (`composeCartLine` with `::`/`·` separators and the frozen `ICartItem`, text-balance in both title branches, the surgical lint regex).
- SSR smoke of all four shapes with CLEAN discriminators (element-boundary `>text<` patterns — plain greps are polluted by the flight-payload dictionary, and `grep -c` counts lines, not occurrences; first pass mis-read, re-run properly): out tee renders size badges + outMsg + description and NO add-to-cart/stepper; minimal patch renders the buy block and NO size/description; set title full; en parity on the sticker.
- Reviewer notes dispositioned: text-balance globality = the planner's own OQ-1 ratification (intended); aria-pressed-vs-radiogroup = ratified spec, evolution point only; describedby/status redundancy + the pre-existing aria-label dup = cosmetic, accepted; bg-white-only over the `*-white` family = agreed (minimal provable hole). New DEF-22: `getCategoryView` localizes the whole sibling product list to read one category name on the product page — add a `getCategoryName` accessor opportunistically (build-time-only cost; any step touching `src/data`).
- No fix round — first PR of the initiative to pass planner verification clean. Browser gate ordered to the user incl. the one flow real data cannot exercise: chips/hint/composite-line require a LOCAL, uncommitted `isAvailable` flip on one tee (catalog data is sacred — flip, test, discard).

## 2026-07-25 — 4e merged and live; DEF-3 CLOSED; D3.4 blocked on one account switch

- User's browser gate passed; PR #10 squash-merged (`a9bab45`). Merged master re-verified (tsc/lint/zero-env build green; composeCartLine/SizeSelector/text-balance all present); live domain serves the new product page (out tee: size badges + outMsg, no buy controls; patch: buy block + in-badge). Step 4e CLOSED; DEF-3 CLOSED with the slug-uniqueness constraint retired; CLAUDE.md updated (size-aware cart identity + frozen ICartItem, SizeSelector in the DS list, the bg-white seal exemption documented).
- D3.4 prep hit a knot worth recording: the kit's checkout strings (order-success "no online payment" note, uk form labels — DEF-14 items, "reserved for the checkout step" in kit-screens-reference.md) were never exported and exist ONLY in the original DS project's `ui_kits/storefront/data.js`; the current /design-login session sees the cumulative project but 404s on `62bf007e` (two-account juggling), and the cumulative dialog cannot re-derive them either (fresh project, no kit mounted). Verified the live dictionaries as the incumbent baseline (full working form vocabulary incl. order_success/order_error). Next action: user switches /design-login to the owning account, planner pulls the strings verbatim, then writes the D3.4 brief — verbatim discipline over re-invention, per DEF-14.

## 2026-07-25 — Kit checkout pulled and archived; D3.4 cart/checkout brief issued

- User switched /design-login; planner pulled `ui_kits/storefront/{data.js,Checkout.jsx}` from the original project. Delta analysis: the live dictionaries already carry ~90% of the kit checkout copy verbatim (the kit copied THEM); the genuinely kit-authored strings are exactly three — successTitle «Замовлення прийнято», the no-online-payment successNote, required «Обов'язкове поле». The kit summary line already concatenates ` · size` — our shipped DEF-3 composed titles make that implementation-free. Everything archived to `design-export/kit-checkout-reference.md` (composition sketch + string delta), closing the reserved gap for good.
- D3.4 brief issued (`design-6-cart-checkout-prompt.md`, same cumulative dialog): the buy-completion pair as one step — cart drawer (Sheet stays; line items with composed sized titles + the long set title as design-load; ConfirmDialog removal; empty state; **add-to-cart feedback explicitly delegated to the designer** — today it is SILENT, a real UX gap surfaced by 4e verification) + checkout page (kit sketch offered; the form field SET frozen as a payload-sacred constraint — restyle, never add/remove/rename; required set = the live six; validation/pending/success/ERROR states all mandated as design-load, incl. cart-survives-error). Accent budget: one CTA per view, the success check-square ruled a status marker. Full copy inline: live strings verbatim + the three kit adoptions.
- Account-juggling gotcha promoted to the board: the cumulative dialog lives on the OTHER account than the current /design-login — the user must re-run /design-login before the planner's D3.4 fidelity review.

## 2026-07-25 — D3.4 planner fidelity review done; two user gates pending (visual + a ratified-page interaction change)

- Both new surfaces read in full; vocabulary 100% D1 kit CSS + tokens (utg-cartline/drawer/scrim/field/label/errtext/input all verified present in components.css — composed, not invented; the checkout toast keyframe and the summary thumbs' `#fff` are prototype glue mapping to our shipped Sonner Toaster and `--white`). Checkout honors every hard constraint: frozen 8-field set with unchanged `name`s, the live required six, error (`role="alert"` + focus-jump) / pending (Button loading) / success (cart clears ONLY here) / error-toast (cart+form survive) all designed; accent budget correct (drawer: proceed; page: place order). Drawer = the shipped Sheet pattern with ConfirmDialog reuse and the empty state. Data module: catalog byte-identical; dict extended with live strings verbatim + the three kit adoptions verbatim (+ tiny new a11y string «Закрити»/"Close" — check the shipped Sheet close label at 4f prompt time). Product-page MARKUP byte-equal to the ratified export — the delta is behavioral only (cart wiring + the flagged interaction change).
- **Flagged to the user as a real design change on a SHIPPED ratified page**: the designer switched the product size chips from the ratified explicit-choice flow (no preselect, toggle-deselect, destructive «Оберіть розмір» hint) to radio-with-first-size-preselected — the hint flow goes dead. Planner recommendation: REJECT for the app (a live shop silently defaulting to size M invites wrong-size orders — order accuracy beats one saved click; the demo-smoothness motive doesn't transfer), keep the prototype's preselect as demo convenience only. Product/UX domain — the user decides.
- Add-to-cart feedback decision (delegated by the brief): the drawer OPENS on add — planner endorses (instant confirmation + checkout path in one gesture, zero invented strings; the brief's toast alternative would have needed new copy).
- DS-addition triage of the designer's four proposals: (1) `CartLine` composite — ACCEPT for 4f (the D-10 pattern; the 32px trash affordance becomes internal to the component, so no IconButton API change); (2) Dialog z-layering token — NOT APPLICABLE (prototype-bundle artifact; the app's Radix portal stack already layers ConfirmDialog over the Sheet, shipped 4a and live); (3) drawer empty-state link — app-land (the live drawer already renders the here-link via NavLink; flag-blue on text links is D1-ratified); (4) checkout summary card — app-land composition (band-colored header row + bordered container are token utilities on layout containers; nothing interactive to seal). Plus one planner-added micro-delta: `IconSize` gains 40 for the empty-state pictogram; the prototype's 1.5 stroke is REJECTED — the D1 2px stroke law wins inside the sealed Icon.
- Export + references + the 4f prompt land after the user's two verdicts.

## 2026-07-25 — D3.4 ratified (preselect REJECTED); exported; D-12 ratified; 4f prompt issued

- User's two verdicts landed: pair approved visually; the designer's radio-preselect on the shipped product page REJECTED — the ratified 4e interaction stands (planner recommendation held; the prototype keeps its preselect as demo convenience, the cart reference records the rejection).
- Bot-source verification closed the D-12 groundwork: `extracted/bot-contract-index.js` destructures known keys only (an added key is ignored — additive-safe) and its `formatPrice(total, locale)` via `currencyMap[locale]` is exactly DEF-13's defect. D-12 RATIFIED (planner, executing the standing DEF-13 disposition; user veto open until the 4f merge): payload gains `currency: "UAH" | "USD"` from the money context; bot-side read is the user's separate follow-up in `utg-tg-order-bot`. DEF-13 site-half scheduled to 4f; DEF-22 re-slotted to the step-5 window (4f never touches `src/data`).
- Bonus find during the same verification: the live Sheet close button is hardcoded `aria-label="Close"` — the kit's «Закрити»/"Close" string fixes a real (tiny) localization gap; `cart.close` rides 4f.
- Exported verbatim: `screens/cart/` (drawer DC + the shared `cart-drawer.js`) + `screens/checkout/` + refreshed `screens/data/catalog.js` and `screens/product/Product.dc.html` (wired versions), each with a distilled reference (cart: CartLine spec + the IconSize-40 micro-delta with the 1.5-stroke rejection + the drawer-opens-on-add ratification; checkout: frozen-form constraints, all four outcomes, D-12 mapping, Field/Input-components-are-the-law note). KNOWN BOUNDED GAP: the wired Home/Catalog/Category exports are deferred to the D3.5 export pass — their delta is demo plumbing (per-screen drawer mounts; the app's drawer is global in the layout), designer-reported as wiring-only and covered by the user's click-through; refresh scheduled.
- `step-4f-cart-checkout-prompt.md` issued (`/feature`): CartLine + IconSize 40, drawer/checkout re-compositions, open-on-add wiring, D-12 currency, four dictionary keys; the natural error-path gate is the zero-env 503 (no mocking needed); outcome semantics preserved (failed order never clears the cart). Expected plan-gate items: CartLine API, key placement, currency threading.

## 2026-07-25 — 4f plan gate approved; icon-size deviations ratified at the sealed union

- Executor's two self-corrections accepted as good hygiene: the commit order fixed itself (dictionaries must precede the drawer — `Dictionary` derives from en.json, so a drawer commit referencing `cart.close` before the key exists fails tsc mid-sequence; final order currency → CartLine → IconSize → dictionaries → drawer → open-on-add → checkout), and the `total`-naming rationale corrected (the live CartLine already renders price × quantity — the ReactNode `total` slot is future-proofing, not a live-bug fix; recorded in its D-1).
- Approved: CartLine API (media/total as ReactNode slots — the DS imports neither Next nor money formatting, mirroring CategoryTile; remove trigger internal at 32px; ConfirmDialog stays app-side on a single `confirming` state, with the explicit onConfirm = removeItem + setConfirming(null) note — the dialog does not self-close); key placement per the incumbent structure (`cart.close`, `cart.required` beside the form vocabulary that historically lives under cart.\*, `checkout.successTitle`/`successNote` beside checkout/summary); zero orphans proven by grep; `currency: money.currency` from `useMoney()` — no mapping, the type is already the union, first commit against the untouched screen.
- Deviations RATIFIED, all three: prototype 18px trash → 20 and 26px check → 24 (the D3.3 Instagram precedent — mock-raw px translate to the sealed IconSize union; 40 enters for the empty-state pictogram only), `noValidate` + kept `required` attributes (native bubbles would preempt the designed error flow; attributes keep the a11y semantics). CheckoutScreen split into four app-land leaves under `src/components/checkout/` at the 300-line bar — accepted, screen-internal composition is app-land.
- Two gate tabs (role=alert, the `state` field) relayed with planner defaults: submit-triggered errtexts keep `role="alert"` (assertive is right after a user action; the 4e hint's `role="status"` stays — different severity), and the `state` form field stays OPTIONAL (the live required six is ratified in the reference).

## 2026-07-25 — 4f gate OQs ruled; a planner truncated-grep error corrected by executor evidence

- OQ-A ruled (b): `role="alert"` lands INSIDE `field.tsx` — a sanctioned THIRD DS change; the fence formally extends by `field.tsx` for exactly this attribute. The 4e text-balance precedent applies (the refinement is DS-owned so every consumer gets it), and option (a) would leave `Field.error` as a typed trap that silently drops the announcement for any future caller — completing the semantics of an EXISTING error API, not new visual language.
- OQ-B ruled: required SEVEN — keep the live behavior. The executor refuted the reference's "six matches the live form" claim with evidence; planner re-verified with an untruncated grep: 14 `required` occurrences = 7 fields × 2, `state` explicitly required (CheckoutScreen.tsx:173). The original claim came from the planner's own `| head -6`-truncated grep — the same failure class as the 4b dialog-branch premise, again caught by executor verification before it shipped. With the premise dead, the merits favor live semantics: the kit's optionality was a designer guess with no operational knowledge, loosening validation on the money path is a real behavior change nobody requested, and region disambiguates same-named Ukrainian cities for the operator. Both reference files corrected in place with the ruling and the error trail; the planner default given in the previous entry ("state stays OPTIONAL") is superseded. User veto stays open — flipping to the kit's six is a one-constant change.

## 2026-07-25 — 4f verified and merged; mid-round amendments; session closes at the D3.5 boundary

- Mid-round, user-caught: the conditional-mount ConfirmDialog amputated the DS exit animation (dialog.tsx carries `data-[state=closed]` fade-out that Radix Presence waits for; the plan's «рендерится условно» was planner-approved — user caught what executor and planner both passed). Amended to controlled-mount with retained payload; the executor's own review then caught the follow-on regression the amendment created (the eternal layout mount lost the implicit reset — fixed by reset-on-drawer-close) plus two more self-introduced regressions (success-transition inside the network try; 320px stepper overflow) — all fixed pre-merge. The field.tsx `id` overstep (second attribute beyond the role="alert" grant, required for aria-describedby to function) RATIFIED post-hoc — flagged transparently as a named PR risk, the process working as designed.
- Planner verification on PR #11 (15 files, 10 atomic commits in the corrected order): battery green (lint = 0 errors + 1 warning in the PLANNER-exported `cart-drawer.js` → DEF-26, eslint-ignore for `initiatives/` rides 4g), seals zero, payload `{...values, cart, locale, total, currency}` exactly D-12, `clear()` single call site, controlled dialog + reset read line-by-line, prerendered checkout band verified in built HTML. One planner-ordered one-line fix before merge: `setIsSuccess(true)` BEFORE `clear()` — a throwing storage write must degrade to a stale cart, never to a duplicate-order path. Landed; user browser gate green; PR #11 squash-merged (`e428fcb`). Merged master re-verified (build 44/44, fix present). Step 4f CLOSED; DEF-13 site half SHIPPED.
- Executor's skipped findings routed: DEF-23 (drawer auto-open focus lands on `<body>` — investigate the Radix mechanism, owner 4g), DEF-24 (USD cent rounding in the summary — informational display, operator charges UAH; step 5/6 or DROP), DEF-25 (autoComplete tokens — `CheckoutField` is the single home now; step 5).
- Boards trued (4f ✅, D-1…D-12, carry-forwards incl. DEF-23…26); CLAUDE.md gains `CartLine`, the D-12 payload line, and the size-aware cart identity note. Session ends here by user call; NEXT SESSION opens with the deferred post-merge prod verification (Vercel deploy on `e428fcb` + live drawer/checkout smoke) before the D3.5 brief — the only verification debt this close leaves open.

## 2026-07-27 — 4f prod-verified live; the deferred verification debt clears

- The deferred post-merge verification opened this session: Vercel shows `e428fcb` deployed READY to production, the two follow-up docs commits likewise; prod serves `d556f04` and every chunk URL is stamped with its deployment id (`dpl=dpl_AspGqu…`) — the live site is exactly the verified merged master. Route battery all-200 on both locales (home/category/product/checkout/reports/about + a catalog image). One host fact worth recording: the apex answers 308 to the `www` canonical — pre-existing domain config; future curl smokes must use `-L` or the `www` host, or every body comes back as a 15-byte redirect stub (this session's first grep pass "failed" exactly that way before the redirect was noticed).
- 4f-specific live claims held: the checkout band prerenders on both locales (`>оформлення<` / `>checkout<` in served HTML — the same discriminator the pre-merge built-HTML check used; the form mounts client-side behind the hydration guard, as composed). The shipped checkout chunk carries the D-12 payload builder — `total:(…).toFixed(2),currency:p.currency` POSTed to `/api/place_order/` — and a zero-side-effect GET probe on that exact trailing-slash URL returns 405, proving the POST-only route is reachable without sending an order. Drawer interactivity was already the user's pre-merge browser gate; optional live re-clicks offered, no planner-side verification debt remains. Next: Desktop snapshot refresh + the D3.5 reports/about/404 brief.

## 2026-07-27 — D3.5 brief issued (reports/about/404); the D3 track's final page brief

- Desktop snapshot re-synced; `design-7-reports-about-404-prompt.md` written into the D3 track — the closing trio as one brief. Reports: kit grid sketch offered as the starting point, swiper explicitly forbidden as a default (a slider would be a DS-addition proposal), hard honesty rule — caption only report #3's FPV fact, bare numbering for the other seven, never invent purchases. About: kit narrow-column sketch; en paragraph 1 adopts the kit "unit we support" fix per DEF-14; the second paragraph's stale future-tense promise of a reports section (live in the nav since 1.0) flagged as a PROPOSED-COPY decision for the owner. 404: kit band composition with the merch CTA to /category kept as the kit ruling, the numeral's survival delegated to the designer.
- Honesty pre-framing in the brief: `SectionBand` carries no intro-paragraph slot today — an in-band reports intro must be listed as a proposed DS addition; accent expectation ZERO on all three surfaces; the runtime-error screen declared an implementation-time reuse of the ratified 404 composition (retry instead of CTA), folded into 4g without its own design surface unless the designer objects. Wiring: this brief COMPLETES the cumulative prototype — header/footer nav routes live, a dead-link demo lands on the 404, the whole site demos on mocked data.
- Next: user carries the brief into the cumulative dialog; the planner reappears at the fidelity review (two-account gotcha stands — /design-login must point at the cumulative project) and writes the 4g prompt after ratification + export.

## 2026-07-27 — D3.5 fidelity-reviewed and ratified (copy change ADOPTED); exported; step-4g prompt issued

- Fidelity review across the cumulative project: the three new screens are token-clean (one mock-raw `#fff` = the 4e-ratified white-matte translation), ZERO DS additions — the reports intro went below the band (the no-extension path the brief pre-framed), the band meta `01–08` rides the shipped SectionBand meta API, inline links ride the drawer's flag-blue precedent. Honesty rules held mechanically (`caption: i === 3 ? reportFpv : null` — one captioned figure, seven bare numbers); copy byte-verbatim incl. the DEF-14 en "unit we support" fix; accents zero on all three surfaces; the 404 numeral died into the `/ 404` kicker with the CTA retargeted to /category per the kit ruling.
- Ratified-screen diff (the designers-slip-changes check): CartDrawer.dc.html + cart-drawer.js + Checkout.dc.html byte-IDENTICAL to the D3.4 export; Home/Catalog/Category/Product carry only sanctioned wiring — drawer mount + on-cart-click, live cart counts, unknown-slug → 404 redirect (mock mirror of prod `dynamicParams=false`), and the brief-ordered `demoDeadLink` toggle (default off); the data module is additive-only (D3.5 strings, existence checks, reports(), nav stubs → real routes). The first D3 round with NOTHING to flag.
- The one owner gate ruled: the proposed about closing line ADOPTED — «Фотозвіти з кожної закупівлі — у розділі звітів.» with a live link replaces the stale future-tense promise in both dictionaries (the reports section it promised has been live since 1.0); en P1 takes the kit "unit we support" fix. Recorded in the about reference; the old sentence dies in 4g.
- Export refreshed wholesale: `screens/{reports,about,404}/` land with distilled references (about records the copy ruling; 404 records the error.tsx-reuse rider and the no-redirect note; reports records the hard caption honesty rule); Home/Catalog/Category/Product/data refreshed with the wiring delta — the deferred wired-export debt CLEARS; the prototype now demos the entire site on mocked data.
- Step-4g prompt issued (`/feature small`): three recompositions + swiper's death (D-9 completes) + four riders — error.tsx alignment to the 404 composition, DEF-21 Home band retrofit (planner call: fold into 4g, zero-visual-delta proof required), DEF-23 focus investigation+fix (DS sheet.tsx or layout ref sanctioned), DEF-26 eslint-ignore making `yarn lint` fully clean. DEF-14/21/23/26 re-dispositioned to 4g; boards trued (D3 track ✅ COMPLETE — all five page designs ratified and exported).

## 2026-07-27 — 4g plan gate: the dead-404 discovery (prod-verified), three OQs ruled, fence extended

- The executor's blocker VERIFIED independently on live prod before ruling: /uk/zzz and /uk/category/nope both serve Next's built-in bare English 404 (the custom title greps ZERO) — with the root layout inside the dynamic [lang] segment, [lang]/not-found.tsx has NEVER rendered. The executor's decisive probe (a forced notFound() in a static page also bypasses it; a nested boundary renders only the **next_error** shell without layout/CSS) takes dynamicParams out of the equation. Today's shop serves unstyled English 404s — the D3.5 surface ships a page that currently does not exist at all, which raises the step's value.
- OQ-1 ruled → 1A (root not-found): root src/app/not-found.tsx thin shell (globals.css + fonts via a new shared src/app/fonts.ts; import-only change in [lang]/layout.tsx) + NotFoundScreen with pathname-resolved locale; the dead [lang] file dies. Ratified deviation recorded in the reference: no chrome on the 404 (outside the [lang] layout/providers — the composition + outline CTA stand alone); planner mandate closes the lang hole — the screen sets document.documentElement.lang on mount. 1E (drop the surface) rejected: it falsifies "every page renders through the DS"; 1B (recompose dead code) rejected outright.
- OQ-5 ruled → yes, dialog.tsx joins the fence. DEF-23's mechanism is CONFIRMED at source (radix-dialog onCloseAutoFocus preventDefaults into a forever-null triggerRef on triggerless controlled dialogs; composeEventHandlers order forces the prevent-and-focus fix shape) and the SAME defect lives in the DS ConfirmDialog — cancel drops focus to <body> INSIDE the drawer's active trap. Same defect, same one-file fix, zero API change. New keyboard gates: confirm-cancel returns focus into the drawer with the trap intact; post-remove focus lands sanely.
- OQ-2 ruled → both system surfaces keep local consts: for the 404 it is forced (outside I18nProvider — useDictionary throws); for error.tsx the boundary must not depend on the context it may be reporting on; the two system surfaces stay symmetric. DEF-14 deviation goes into the PR text; reports/about strings land in dictionaries per plan.
- Minor executor calls approved as proposed: the about {link} token inside the existing all_proceeds key (the remove_body substitution precedent, byte-match verified); reports images alt="" with visible numbered captions, zero priority + all lazy (step-5 Lighthouse arbitrates LCP), quality untouched; DEF-21 through the existing #merch wrapper section (the id stays app-land — the DS API is frozen), with the four predicted deltas as the zero-visual-delta proof.

## 2026-07-27 — PR #12 planner-verified; six-item fix round ordered; three new DEFs

- Independent verification on `feat/reports-about-404` (PR #12, 14 commits, 18 files, zero planner-artifact leaks; one benign out-of-fence widening: `utils/locale.ts` gains a `DEFAULT_LOCALE` export for the 404 screen): tsc green, `yarn lint` FULLY clean (0/0 — DEF-26 works), prettier clean, zero-env build 44/44 with the route table intact, all seal greps zero, swiper zero across src/package.json/lockfile, the old about sentence greps zero, `[lang]/not-found.tsx` gone. The built static 404 verified directly: `UTG | 404` title, `<main>` landmark, `/ 404` kicker, uk strings, bare `<html>` (the recorded deviation). Barrel-seal spot-check: `useReturnFocus` is NOT exported — the building block stayed internal. Home retrofit read line-by-line: wrapper section keeps `id="merch"`+scroll-mt, SectionBand as=h2 title+meta — the documented deltas only.
- The reviewer's two must-fix findings CONFIRMED at source before ruling: (1) the reports `sizes` three-column threshold used one gutter — the formula itself subtracts two (`33.33vw−53` ⇒ −112px−48px over 3), theme gutter is 56px at ≥768, so the breakpoint is 940, not 884; the other five segments re-derived exact. (2) `splitAtToken` returns `[text, ""]` on a missing token, so the link renders GLUED to the paragraph end — worse than the PR's stated "silently loses its link"; the contract becomes null → paragraph renders linkless. Both hook hardenings confirmed real (module-scope `lastInteraction` retains a detached node until the next interaction; `FOCUSABLE` matches `tabindex="-1"` and disabled buttons — a path back to the very bug the hook kills).
- Fix round ordered (six items, all in-fence): sizes threshold 884→940; splitAtToken null-contract + conditional link + true the PR Risks wording; null out `lastInteraction` after use; tighten `FOCUSABLE` with `:not([disabled])`/`:not([tabindex="-1"])`; rename `reports.fpvCaption` → `fpv_caption` (dictionary snake_case convention); drop the dead `max-w`/`gap` wrapper utilities around the single error.tsx button (adding a body line would be invented copy — rejected).
- Routed to the backlog: DEF-27 (the hook's pathname guard is app-routing knowledge inside the sealed DS + module-scope listeners without teardown — the `defaultPrevented` bail is the future app-side override path), DEF-28 (`SectionBand.center` lost its last consumer — dead prop in a frozen API, removal needs a sanctioned window), DEF-29 (the 404 shell's lang-less `<html>` pre-hydration; `experimental.globalNotFound` noted). The alt="" SR trade-off recorded in the reports reference where the next implementer will see it.

## 2026-07-27 — the user's browser gate broke the 1A architecture; probe → catch-all-page ruling; round 3 ordered

- The gate caught what planner, executor AND reviewer all missed: navigating OUT of the root 404 mounts the [lang] layout's `<html>` inside the auto-generated root layout's `<body>` — nested-html hydration errors (the root not-found's existence materializes a DefaultLayout segment above [lang]); plus the uk-first flash on direct /en dead URLs (one static shell serves both locales). Second user-caught miss of the initiative after the 4f dialog animation — the browser gate keeps earning its keep. My 1A plan-gate ruling was the wrong call: the click-out path was never probed.
- Planner probe (catch-all + [lang]/not-found boundary, then reverted): the boundary IS reachable through a `[...rest]` catch-all — the plan-gate "dead [lang]/not-found" finding holds only while no route triggers it — and it returns true 404 status; but a direct load still renders the `__next_error__` shell WITHOUT the layout (no chrome, client-only locale = the same flash), while the boundary bakes into every SSG page's HTML for client-side navigation (styled and chromed there). Server-side locale inside the boundary is off the table: it prerenders into every static page, so a dynamic API there would de-SSG the site.
- RULING: the 404 becomes an ordinary PAGE — `/[lang]/[...rest]/page.tsx` renders the ratified composition inside the real layout (chrome RETURNS — closer to the approved prototype than the 1A deviation), server-rendered per-request in the URL's own locale (the flash becomes impossible), `robots noindex` + the 404 title via generateMetadata. The root `app/not-found.tsx` DIES and takes the nested-html class with it; the orphaned `fontRootStyle` export goes too. RECORDED TRADE-OFF: locale-prefixed dead URLs now answer 200+noindex (soft-404) — step 5 revisits or accepts; middleware-excluded residue (dotted paths, unknown locales) keeps the built-in bare 404, a crawler-only surface. OQ-2 flips for the 404 alone: inside the provider the strings belong to the dictionaries (DEF-14 restored); error.tsx keeps local consts — the boundary-robustness rationale stands. The defensive category/product notFound() guards stay: unreachable with a static catalog, and the catch-all wins those URLs first (probe-verified).
- The reports lightbox request (owner, at the gate) routed to a 4h micro-step after the 4g merge: a clickable media surface must enter the sealed DS (raw `<button>` is lint-banned in app-land), and a DS addition needs its own ratification and PR — not a graft onto a closing one.

## 2026-07-27 — round 3 planner-verified; PR #12 merged on user delegation; step 4g CLOSED; D3.6 brief issued

- Round-3 verification on 27593f0: battery green (lint 0/0, zero-env build 44/44 + exactly one new route row `ƒ /[lang]/[...rest]`), the catch-all page correct (async params, `robots noindex` + the 404 title via generateMetadata), the root shell and `fontRootStyle` gone, NotFoundScreen a plain dictionary screen (no pathname parsing, no effects). One byte-match alarm dissolved on inspection: the reference .md line-wraps the en body, so a substring check fails — against the TRUE SSOT (the exported prototype `catalog.js`) the dictionary is byte-exact. Prod-server smoke on :3311 confirmed every claim first-hand: all four dead-URL classes serve the full layout (header present), native per-locale `<html lang>` with server-rendered strings and zero cross-locale leakage, noindex + title, locale-prefixed CTA; /uk/reports intact. The executor also independently confirmed the plan-gate probe result (catch-all wins even exact-shape URLs like /uk/category/patches/nope — `dynamicParams=false` cascades).
- MERGED: the user delegated the merge explicitly this round; squash-merged as `300333d`, merged master re-verified (build green, swiper grep zero). Step 4g CLOSED after three fix rounds — every page of the live shop now renders through the sealed DS, and the D-9 retirement list (Flowbite, notyf, body-scroll-lock, swiper) is fully executed. The executor also self-corrected the PR description when the round-3 pivot made it lie ("если описание ПР врёт — исправлять", user directive worth keeping).
- Deferred: DEF-14 CLOSED (the design-copy pipeline completed 4c→4g), DEF-21 CLOSED (zero-delta band retrofit), DEF-23 CLOSED (return-focus in Sheet AND Dialog; hygiene residue lives on as DEF-27), DEF-26 CLOSED (lint fully clean). DEF-29 reworded to its live remainder: soft-404 (200+noindex) on locale-prefixed dead URLs; terminal fix = a real root layout owning html/body; step 5 revisits or accepts. notfound-reference and CLAUDE.md trued to the final catch-all architecture.
- D3.6 issued (`design-8-reports-lightbox-prompt.md`, same cumulative dialog): the owner-requested reports lightbox — clickable figures + an image-first viewer, every needed primitive an explicit DS proposal (Dialog is a closed intent API; a lightbox is a new intent), same-asset honesty (no hi-res originals; crop decision delegated and must be stated), caption honesty carried over (03 only), prev/next delegated with a no-carousel fence, accent budget zero. Implementation = micro-step 4h after ratification.

## 2026-07-27 — 4g prod-verified live; no verification debt carried

- Vercel: `300333d` READY in production (the trailing docs commit building behind it, app-identical). Live smoke on www: /uk/zzz and /en/zzz serve the catch-all 404 with full chrome, per-locale `<html lang>` and server-rendered strings, noindex + the 404 title (status 200 — the recorded DEF-29 trade-off, now live); /uk/reports carries the intro, 8 figcaptions, exactly one FPV caption at the element boundary and the corrected 940px sizes threshold; /uk/about links the reports pointer to /uk/reports with the old future-tense sentence gone from the served bytes; swiper zero on the live home. Step 4g fully closed, nothing deferred to a next-session opening this time.

## 2026-07-27 — D3.6 planner fidelity review done; three DS proposals triaged ACCEPT; one copy question at the user gate

- Fidelity on the delivered lightbox (edits confined to Reports.dc.html — the shared catalog.js re-pulled and read against the export, byte-identical, zero new strings): token-clean throughout with known translations only (#fff = the white-matte token, rgba-ink scrim = the shipped Sheet scrim, figure focus ring = the SizeSelector secondary-ring precedent, band-context controls = paper-outline focus + inverse hover, zoom on --dur/--ease). Honesty held mechanically — the lightbox derives from the same reports() accessor, header strip shows «03 — FPV» verbatim and bare indices elsewhere. The ratified grid composition intact under the delta (107 lines, all additive: figures become buttons, the viewer joins the screen). Raw <button>s declared as presentational prototype vocabulary (the SizeChips precedent) — they map to the proposed DS additions. Anti-carousel honored: clamped ends (disabled at 35%), no wrap — D-9 stays dead.
- Proposal triage (planner ACCEPT, ratification at the user gate): (1) MediaFigure — the missing clickable-media control, caption row stays outside it; (2) Lightbox — a NEW closed intent, image-first (ink header strip + uncropped contain on white + 2px frame), NOT a Dialog API extension — implementation composes the DS-internal Radix Dialog primitives, which brings the focus trap, scroll lock, Esc and the return-focus law the prototype hand-rolls; (3) prev/next as Lightbox-INTERNAL controls (not a public primitive) + the Icon set gains the chevron glyphs (missing today — designer inlined Lucide paths, declared). Uncropped-vs-square ruled by the designer as uncropped-contain — the stated rationale (the grid crops, the viewer reveals) accepted.
- Two planner riders for 4h: the prototype labels prev/next with the NEIGHBOR INDEX («02») and empty-when-disabled — functional but weak SR vocabulary; proposal: two new dictionary keys (uk «Попередній звіт»/«Наступний звіт», en "Previous report"/"Next report"), planner-authored copy, user adopts or vetoes at the gate (the 4f `close` precedent). And the lightbox img alt duplicates the dialog aria-label (double announcement) — implementation sets alt="" inside the viewer, the header strip IS the visible text; executor plan-gate item.
- User visual gate pending; ratification + export + the step-4h prompt follow it.

## 2026-07-27 — D3.6 ratified (user visual gate green, labels delegated); exported; step-4h prompt issued

- User approved the lightbox visually and delegated the open copy call; planner ruling: the two label keys ADOPTED — «Попередній звіт»/«Наступний звіт», "Previous report"/"Next report" — a neighbor-index aria-label reads as a bare number to a screen reader, and the 4f `close` precedent already made control labels dictionary citizens. Both prototype rejections recorded in the reference (index-labels, viewer alt duplication).
- Ratification bundle: the three DS proposals stand sanctioned (MediaFigure, Lightbox as a distinct intent composed on the DS-internal Radix primitives, prev/next as Lightbox-internal + the Icon chevron gap); uncropped-contain ruled in; clamped no-wrap semantics keep D-9 dead. Exported: the D3.6 `Reports.dc.html` revision verbatim over the D3.5 one (grid intact, viewer added) + the reference gains the "Lightbox (D3.6 addendum)" spec with the mock-plumbing do-not-copy list.
- Step-4h prompt issued (`/feature small`): Icon glyphs + the two composites + ReportsScreen wiring + the two keys; plan-gate OQs expected on the component APIs, the actually-missing chevrons, and the Radix composition; fence excludes `useReturnFocus` (DEF-27 stays parked). The D3 track now closes for good — every designed surface incl. the addendum is ratified, exported, and either shipped or prompt-issued.

## 2026-07-27 — PR #13 planner-verified; aria-disabled RATIFIED at source; four-item fix round; DEF-30..33

- Independent verification on `feat/reports-lightbox` (PR #13, 4 commits, 8 files = exactly the fence, zero planner-artifact leaks; DialogOverlay stays off the barrel — internal-only, seal intact): battery green (lint 0/0, zero-env build with /[lang]/reports still SSG), the static grid unchanged in substance (8 figcaptions, exactly one FPV caption, composed aria-labels server-rendered), the two label keys byte-match the adopted copy, and all 8 REPORT_DIMENSIONS pairs re-verified against the actual JPEG headers (planner-run struct parse — MATCH across the board).
- The deviation RULING — aria-disabled stays, ratified over the prototype's disabled: every clause of the review's rationale confirmed at Radix source — focus-scope's focusout guard returns on `relatedTarget === null` (a just-disabled focused node fires exactly that) and its MutationObserver watches `{childList, subtree}` only, so the disabled attribute would strand focus on <body> with no recovery; the executor's earlier effect-patch provably broke onOpenAutoFocus. Visual/behavioral contract preserved (35% opacity, pointer-inert, activation guarded in step()). The reference gains nothing — the PR documents it; the acceptance wording "disabled chevron inert" is satisfied semantically.
- Review findings verified and a four-item fix round ordered (all lightbox.tsx): (1) dead aria-label — Radix sets aria-labelledby={titleId} BEFORE the props spread (dist:225-227), so the accessible name today is the bare index; fix = aria-labelledby={undefined} alongside the label (the spread-override trick already used for describedby). (2) Radix's getTabbableCandidates filters disabled but not aria-disabled — opening report 01 lands focus on the inert prev chevron (a direct ricochet of the ratified deviation); fix = onOpenAutoFocus: captureOpener + preventDefault + focus the panel (Content already carries tabIndex=-1 and focus:outline-none). (3) stepping is silent for SR users — the header text changes with focus parked on a chevron; fix = aria-live="polite" on the title node (reconstructed from the review's truncated third item — "one attribute"). (4) rename handleMediaMouseDown → preventMediaDrag (the no-comments rule makes intent-bearing names the only documentation channel). The double-clamp nit REJECTED as dead code: the Lightbox guard is the composite's own contract, the screen clamp is the app's — a DS composite must not rely on app-side clamping.
- The six honestly-open findings routed: DEF-30 (close-label borrowed from cart.close — hidden coupling, the review's top pick), DEF-31 (viewer loading polish), DEF-32 (IconButton inert state + the forked scrim — one DS hygiene row beside DEF-27/28), DEF-33 (REPORT_DIMENSIONS drift-guard → a step-6 unit test). No-tests-in-this-step accepted — the suite is step 6's charter scope, not a micro-step graft.

## 2026-07-27 — 4h fix round planner-committed on delegation; PR #13 merged; step 4h CLOSED

- The fix-round delta read line-by-line in the working tree before commit: exactly the four ordered items, correct shapes (captureOpener before preventDefault before panel focus; aria-labelledby={undefined} beside the label; aria-live="polite" on the title node; the rename). Executor judgment calls endorsed: Title stays mounted (the Radix dev-warning resolves titleId via getElementById) and the name/live-region separation makes fixes 1 and 3 compose instead of collide; aria-atomic skipped with a correct rationale. Battery green on the tree. The executor tab had gone idle without committing — planner committed the agreed message and pushed on the user's merge delegation.
- PR body trued before merge (the standing "a lying PR description gets fixed" directive): the deviation block rewritten from open-question to the fixed ruling (aria-disabled ratified at Radix source) + the three follow-up hardenings; the five open findings annotated with their DEF-30..33 numbers. Squash-merged as `71dbb63`; merged master re-verified (build green, reports SSG). Step 4h CLOSED — the D3 track and the whole 4x implementation arc are complete: every surface of the live shop, including the addendum interaction, renders through the sealed DS.
- CLAUDE.md gains MediaFigure and Lightbox in the composite list WITH the aria-disabled law inline (the one non-obvious constraint a future DS contributor would otherwise revert as a "cleanup"); plan.md gains the 4h row. Next: prod verification on `71dbb63`, then the step-5 SEO prompt — its window owns DEF-22/25/30/31 and owes decisions on DEF-4/24/29.

## 2026-07-27 — 4h prod-verified live; the 4x arc closes end-to-end

- Vercel: `71dbb63` READY in production (docs commit building behind, app-identical). Live smoke: /uk/reports serves the MediaFigure grid (8 figcaptions, the composed FPV aria-label on 03 at the element boundary) and both new label keys reach the client in the served bytes; /en mirrors (bare "reports 01" label, English prev/next). The viewer itself is client-side — its keyboard/SR contract was the user's browser gate, passed pre-merge. No verification debt carried.

## 2026-07-27 — step-5 gate rulings (DEF-4/24/29); bot phase parked by user; step-5 SEO prompt issued

- User sequencing recorded: the `utg-tg-order-bot` currency read (DEF-13's second half) is deliberately PARKED until the app work completes — a dedicated bot-polish phase follows steps 5-7. Per-turn reminders stop; the parked state lives in deferred/state.
- The three decisions the step-5 window owed, ruled: DEF-4 → an in-route per-IP in-memory rate limit (module-scope store, 429, executor proposes numbers; WAF dropped as plan-dependent; the limiter rejects before forwarding — the sacred payload is untouchable). DEF-24 → DROPPED (the USD figure is informational; the operator charges UAH — cent-precision there buys nothing). DEF-29 → DROPPED-as-accepted (the soft-404 stays: noindex ships, the sitemap lists only real pages, and the terminal root-layout fix would trade the native per-request `<html lang>` — the root layout cannot read the locale param — for a status code on a 12-product shop; Search Console pain reopens it, nothing else).
- Step-5 prompt issued (`/feature`, `step-5-seo-prompt.md`): metadata per page+locale on the www metadataBase (the apex-308 gotcha baked in), sitemap (38 entries, checkout + 404 excluded), robots, minimal-truthful Product JSON-LD with UAH offers regardless of display locale (the operator charges UAH — the D-12 logic extended to structured data), plus the five riders (DEF-4/22/25/30/31). Copy discipline pre-framed: meta descriptions derive from EXISTING dictionary copy; genuinely new strings go to owner ratification at the plan gate — the DEF-14 no-invention rule extended to search snippets. Honesty in structured data made explicit: no invented ratings/brand/GTIN, absent descriptions omitted.
- Memory hygiene check on resume-adjacent files: the roadmap memory is already a clean pointer to `initiatives/` — no drift, untouched.

## 2026-07-27 — step-5 plan gate approved; three OQs ruled; SITE_NAME ratified

- The gate's headline verified claims endorsed: child openGraph REPLACES parent (executor verified at Next source, resolve-metadata.js:183) — the layout-invariants + buildPageMetadata() architecture is therefore load-bearing, not stylistic; zero new copy strings (metadata derives from footer.mission/reports.intro/about.site_created and composed ratified leaves; titles via capitalize() over the nav chips — lowercase there is a CSS-presentation decision and <title> has no CSS layer, argument accepted). The one new constant SITE_NAME = "Ukrainian Tactical Gear" RATIFIED with the user's approve (locale-neutral, the shop's actual name — lives in code, not the dictionaries). Honest downgrades approved: twitter summary card (a large card would lie with our assets), sitemap without lastModified/changeFrequency/priority (a build-time date is a lie; Google ignores the rest).
- OQ rulings: (1) DEF-25 total-Record shape APPROVED — a total `AUTOFILL_TOKENS: Record<CheckoutFieldName,...>` read by CheckoutField matches the file's existing isRequiredField(name) pattern and makes a forgotten token a compile error; this satisfies the prompt's intent (single home, derived attribute) with stronger guarantees than a prop. (2) The trailing-slash 308 on the LIVE ORDER PATH — take it: CheckoutForm.tsx:67 joins the fence for exactly one character (planner corroborated from the 4f verification: the shipped chunk POSTs to /api/place_order/ and the config has no trailingSlash — every real order pays a redirect hop before the handler). (3) brand + itemCondition ALLOWED in the JSON-LD: "Ukrainian Tactical Gear" is not an invented brand — it is the shop's own name, printed on the shirts and present verbatim in the sacred catalog descriptions; the prompt's blanket "no brand" was over-broad and the executor's escalate-instead-of-include was the process working. itemCondition NewCondition is a plain fact.
- Lighthouse: no Chrome/lighthouse binary in the executor environment (probed, not assumed) — the SEO ≥ 90 gate moves to the user's devtools run at the browser gate, explicitly listed in the PR. src/utils/seo.ts accepted as a fence widening (utils is the established home for cross-cutting helpers). Twelve atomic tasks approved; task 11 (the slash) now unblocked by ruling 2.

## 2026-07-27 — PR #14 reviewed; two blockers verified at source; five-item fix round; tel dropped, street-address and 307 ratified

- The reviewer's merge-blockers CONFIRMED at source before ruling: (1) the lightbox skeleton wrapper is a full-width block div while the letterboxed portrait img renders narrower under object-contain — the pulsing block sits ~270px wider than the photo it replaces on seven of eight reports; fix = the wrapper takes the SAME width formula the sizes attribute already computes (mx-auto + min(100%, calc((92vh−60px)·aspect))), killing the duplicated-formula smell as a bonus. (2) Next's srcset filter regex `/(^|\s)(1?\d?\d)vw/g` (get-img-props.js) cannot match `92vw` immediately after `min(` — percentSizes comes back empty and ALL 16 candidates ship, the opposite of the commit's perf claim; fix = reorder min() args so `92vw` follows ", " (min is order-independent, the regex is not).
- Token rulings on the two open questions: `telephone` DROPS its autofill token — autocomplete is a semantic assertion and `tel` is FALSE for a dual-purpose phone-or-telegram field; the failure asymmetry decides (a phone injected over a typed handle is visible; an @handle saved into the browser profile as a "phone" travels to every other site); type="tel" keeps the keyboard. The true fix (splitting the field) REJECTED — it would break the sacred payload shape for a convenience. `address` KEEPS `street-address` — address-line1 silently drops building/apartment and a Nova Poshta order without them does not arrive; the spec's multiline grouping is advisory and all engines fill single-line inputs; the silent-incomplete failure mode is the worse one.
- Also in the round: the limiter fails OPEN on missing identity (the shared "local" bucket dies — a false 429 costs a volunteer order, and on Vercel x-forwarded-for always exists, reviewer-verified against Vercel docs); siteOgImage() collapses to a module constant. The / → /uk 307 RATIFIED as deliberate: the destination varies by Accept-Language, a permanent redirect would let caches pin the wrong locale.
- Routed: DEF-34 (IMAGE_DIMENSIONS second source of truth — terminal fix moves dimensions into the typed catalog schema; step 6 together with DEF-33 as one drift-guard family), DEF-35 (untyped locale list in four places incl. proxy.ts — LOCALES export, step 6). Meta-description overshoot accepted as informational (the no-new-strings rule holds; SERP truncation is cosmetic). The reviewer's independent confirmations (twitter autofill from OG at postProcessMetadata, all 13 dimension pairs, the total-Record compile guarantee, PUBLIC_FILE passthrough, Vercel header trust) logged as corroborated.

## 2026-07-27 — fix round verified; PR #14 merged on delegation; step 5 CLOSED with Lighthouse SEO 100

- Fix-round delta (5 commits) verified line-by-line before merge: the min() reorder restores srcset filtering, the skeleton takes the photo's own width through the shared viewerHeightBoundWidth helper (one formula, two consumers — the reviewer's bonus made a requirement), the limiter's fail-open became a TYPED null contract (string | null — the shared bucket is now unrepresentable), "tel" left both the map and the AutofillToken union (reintroduction is a compile error), SITE_OG_IMAGE a constant. Battery green on HEAD; built output re-verified: sitemap exactly 38 locs, robots built, product JSON-LD carries brand/NewCondition/UAH, canonical on www, all three hreflang alternates (camelCase hrefLang is React SSR serialization — HTML-legal, Google-readable).
- PR body trued (both Risks questions marked RESOLVED with the rulings) — planner-edited per the standing directive. USER browser gates passed with Lighthouse SEO 100 on every page; squash-merged as `095caad` on delegation; merged master rebuilt green (46/46). Step 5 CLOSED; DEF-4/22/25/30/31 CLOSED with dispositions.
- CLAUDE.md gains the SEO section (buildPageMetadata + the replace-not-merge openGraph law, sitemap/robots, UAH-always JSON-LD, SITE_NAME) and the rate-limiter line on the API route. Boards trued; next: the step-6 prompt (tests + CI) with the DEF-33/34/35 riders and the DS-hygiene-trio decision. Rich Results on the live product URL stays an optional post-deploy user check.

## 2026-07-27 — step 5 prod-verified live; session closes at the step-6 boundary

- Vercel: `095caad` READY in production (docs commit building behind, app-identical). Live smoke: /sitemap.xml serves exactly 38 locs, /robots.txt with the sitemap pointer and the /api disallow, the live product page carries canonical on www + x-default hreflang + JSON-LD with the ratified brand and UAH offer, checkout carries noindex. No verification debt; the streak holds.
- Session closes here by user call. The day's ledger: 4h ratified→implemented→merged (PR #13), step 5 designed→gated→merged (PR #14) with Lighthouse SEO 100 on every page, the bot phase consciously sequenced after the app. NEXT SESSION opens with the step-6 prompt (tests + CI) — riders DEF-33/34/35, the DS-hygiene-trio decision, and after it only step 7 (README) separates the initiative from its close.

## 2026-07-28 — pre-compact chores: handoff refreshed, step-6 prompt issued, the DS trio ruled into 6b

- Context compaction ahead; everything load-bearing promoted first. The handoff prompt refreshed to the practiced reality: per-round merge delegation (incl. committing an executor's verified-but-uncommitted round), the fix-the-lying-PR-body standing directive, the prod-verify step folded into the loop's step 4, DEF-26 staleness corrected (eslint ignores initiatives/ since 4g), the www-canonical curl gotcha and the parked bot phase written in, the D3 track marked complete.
- Step-6 prompt issued (`/feature`, `step-6-tests-ci-prompt.md`) while the full step-5 context was still warm: Vitest+RTL floor over the money path (composeCartLine identity, the RAW-array persistence codec with legacy-line compat, the resolveMoney fallback matrix, checkout validation + the PAYLOAD CONTRACT as an executable test + success-only-clear semantics, the rate limiter incl. fail-open), Playwright against the zero-env prod server (the real 503 as a deterministic error fixture + a route-mocked 200 success — the suite never touches the real relay), secretless GitHub Actions where THE PR ITSELF must show its own CI green (the charter's acceptance mechanism proving itself), and the prove-then-revert protocol for the contract and drift-guard tests. Riders: DEF-34 (dimensions into the typed catalog schema; IMAGE_DIMENSIONS dies), DEF-33 (headers drift-guard incl. REPORT_DIMENSIONS), DEF-35 (LOCALES export consumed by proxy/sitemap).
- RULED: the DS-hygiene trio DEF-27/28/32 lands as step 6b — a dedicated micro-step immediately after the suite exists, so those refactors go in under green tests; deliberately kept out of 6 to keep the infra step focused. Boards trued; the next action is the user's carry.

## 2026-07-28 — step-6 plan gate: the 15-task plan approved; DEF-34 ratified as `imageSize`; the Node question opened

- Gate A approved as presented. The plan's own calls held up under review and three beat the prompt: (1) `tests/` + `e2e/` at the root instead of colocation — the seal lint bans color/text-size literals in any Literal under `src/**` and `eslint-disable` is a banned comment, so a colocated fixture could become unfixable; the Next page/route matcher verified incapable of ever routing a `*.test.ts`. (2) Two Vitest projects split by a `.dom.` filename infix — the `@vitest-environment` docblock alternative IS a comment, i.e. repo-illegal; globals stay off because `globals: true` forces a tsconfig `types` array that kills `@types/*` auto-include under `next build`. (3) Zero new app exports — every private symbol is reached through public surface (the cart codec via `persist.rehydrate()`, future-timestamps via a backwards `vi.setSystemTime`, LRU eviction via 1001 keys, `splitAtToken` via an RTL render with a synthetic dictionary). Hermetic-by-construction e2e (`webServer.env` blanks the relay), the hand-rolled image-header reader proven against all 23 files under `public/` at design stage, one CI job from cheap to expensive, one PR. Four fence exceptions sanctioned: H-1 `REPORT_DIMENSIONS` → `src/data/reports.ts` (exactly the plan-gate case the prompt demanded instead of a test-only export), H-2 `LOGO_SIZE` + required `OgImage` width/height, H-3 generated-catalog ignores, H-4 the four scripts.
- DEF-34 shape ratified — the executor's third option beats both planner candidates (the planner's nested `image: {src,width,height}` included): `image: string` stays, a REQUIRED `imageSize: ImageSize` lands on `CatalogProduct`/`ProductView` only. The frozen `ICartItem` and live `utg-cart-v2` carts are untouched, zero call-site edits, both OG builders keep their spread, and one `ImageSize` type serves catalog + reports + logo + drift-guard; the image/imageSize desync risk is exactly what the DEF-33 drift-guard closes. The category-type asymmetry is honest: category OG uses the logo, and category tiles render through `fill`.
- Two owner DoD additions accepted: a built-HTML grep for `og:image:width`/`og:image:height` (research warned the `IMAGE_DIMENSIONS` removal silently drops the tags — the very defect DEF-34 exists for), and the full home → category → product click path. Planner riders: the click path is assigned to the real-503 spec only (mock-200 and persistence may deeplink — navigation covered once, no suite bloat); `webServer.env` blanks ALL THREE env keys so a future en-locale spec cannot flake on live rates; and CI's Node major must match the prod build — the Vercel project was found still on 20.x (EOL), first instruction said "bump the dashboard to 22.x to match the CI pin" (corrected two entries down: the evidence names 24).

## 2026-07-28 — PR #15 reviewed: Actions green, the Vercel check RED; D-13 Node-24 ruling; the fix round issued

- The step-6 run delivered 265 units + 11 e2e specs + secretless CI; Actions run 30381026514 green in 2m03s — and the review caught the one gate that repeats prod: the Vercel preview FAILS in `yarn install` (jsdom@30 `engines ^22.22.2 || ^24.15.0 || >=26` vs prod Node 20.20.2; yarn 1 hard-fails on engines). Planner re-verified everything at source before ruling: the deploy log carries the engines error AND Vercel's own deprecation notice mandating 24.x by 2026-10-01; `tsconfig.json` really includes `tests/`/`e2e/` in the prod program; Header really hardcodes the English `Cart:` aria-label; both fragile e2e selectors present; `webServer` serves the PREBUILT `.next` via `yarn start`, so the CI-served build is baked by the unblanked build step — every reviewer claim reproduced.
- D-13 RATIFIED (see decisions.md): one Node major (24), pinned by `engines.node` in package.json as the single binding pin — Vercel reads it and it overrides the dashboard, yarn 1 hard-enforces it, CI consumes it via `node-version-file: package.json`. The reviewer's `.nvmrc` scheme rejected on a verified fact: Vercel does not read `.nvmrc`, so it binds nothing and adds a second drift surface. Sequencing: the executor pushes the pin FIRST and the preview must go green while the dashboard still says 20.x — the PR itself proves the repo pin binds Vercel; the dashboard click to 24.x follows as belt-and-braces. The planner's plan-gate "22.x" instruction stands corrected in the open: 22 only postpones the same cliff.
- The executor's own saves logged with respect: `reuseExistingServer: true` returned before the child-process env was assembled — the blanked `PLACE_ORDER_URL` would never apply, and a dev with real keys + a warm server would have shipped a REAL volunteer order from the suite (now unconditionally `false`); the Vitest globs silently skipped `Foo.test.tsx`/`.spec.ts` shapes (a written-but-never-executed test under green CI — the worst failure a suite can have); six vacuous asserts rewritten and proven non-vacuous by the break-then-revert protocol.
- Fix round issued (`step-6-fix-round-prompt.md`, 8 items): the D-13 pin family (engines + `node-version-file` + `@types/node` ^24), the tsconfig split (tests/e2e/configs leave the prod TS program — closes the executor's honestly-declared risk; the prod build stops typechecking test tooling), CI build-step env blanking (the served build becomes hermetic in CI exactly as locally), the Header aria-label composed from existing `cart.cart` strings (an English accessible name on a bilingual site — and the e2e selector was cementing it), `data-testid` e2e hooks (no DS-internal classes, no accessible-name coupling), the `configDefaults.exclude` spread, the vacuous 503 assert made explicit, and the PR body corrected to the full truth (Actions green AND the deploy check red at review). Routed to the ledger: DEF-36 (the limiter's per-IP bucket shared across the e2e run), DEF-37 (the relay-forwarding path uncovered end-to-end), DEF-38 (the cart codec guard/catch redundancy → 6b). Accepted as-is with rationale: `ImageSize` stays in `catalog.types.ts` (data-layer shared shape; a move buys nothing), the jsdom `quality "100"` stderr noise (env artifact — `next.config.mjs` declares `[75,100]`, jsdom just never loads it), the hand-rolled header parser (reviewer verified the bounds arithmetic independently). Housekeeping: decisions.md's D-6 (Recoil→Zustand) had lost its header and drifted to the file tail as an orphan — restored in place.

## 2026-07-28 — fix round verified byte-level; PR #15 MERGED; step 6 CLOSED; prod live on Node 24

- All 8 fix-round items confirmed on the diff before blessing: engines/`node-version-file`/`@types/node` ^24 exactly as ruled; the tsconfig split with `typecheck` running both programs; the CI build-step env block; the Header label composed from `cart.cart`; `data-testid` hooks with the count in a data attribute (the `Price` wrapper is app-land — DS untouched, the right move under closed props); the `configDefaults` spread; the vacuous 503 assert DELETED rather than replaced — accepted deviation, the explicit `not.toHaveBeenCalled()` already stood one line above and a replacement would have duplicated it. The PR body's self-correction section ("It was not") is the fix-the-lying-PR directive executed unprompted.
- The D-13 proof mechanism worked exactly as sequenced: the deploy log at dashboard-still-20.x says `"20.x" will not apply, "24.x" will be used` plus `Skipping build cache since Node.js version changed` — the repo pin alone binds production. Planner battery green on the branch AND on merged master (265/265, both TS programs, 46/46 pages, both proof greps 0). Squash-merged by the user as `208e192`; the remote branch auto-pruned.
- Prod live-verified minutes after the deploy went READY: `Кошик: 0`/`Cart: 0` aria-labels serve localized, en prices are real USD (`$6.68` — Vercel-env rates alive), og:image 960×1280 on the product page (DEF-34's og dimensions survived the `IMAGE_DIMENSIONS` death), JSON-LD still UAH, sitemap 38 locs, robots intact. No verification debt; the streak holds.
- Mechanics lesson for the handoff: git REFUSES to overwrite an untracked file with tracked content even when byte-identical — the restore-the-prompt-on-the-branch trick therefore requires `rm` of the untracked copy before anyone returns to master (it blocked the user's own checkout; fixed in seconds). The user's mid-close question — UAH prices on BOTH locales locally — answered as designed behavior: zero-env runs have no rates, so both locales honestly show ₴ (D-8; never `$` on a UAH magnitude); prod en is USD, verified live.
- Closed: DEF-33/34/35 with dispositions. CLAUDE.md trued: the "no tests and no CI" line replaced by the suite/CI reality, the three test commands documented, a Tests & CI section added, `imageSize`/`reports.ts`/`LOCALES` written into Data/routing, and the stale "barrel incl. `resolveLocale`" claim fixed (it lives in `src/utils/locale.ts`). Step-6b prompt issued (`step-6b-ds-hygiene-prompt.md`): the DS quartet DEF-27/28/32 + DEF-38 under green tests, zero visual delta, the prompt itself the sanctioned D-10/D-11 window.

## 2026-07-28 — step-6b plan gate: approved with both deviations; four claims verified at source

- Both flagged deviations RATIFIED. (1) Dialog gets NO `onCloseAutoFocus` slot: no Dialog consumer has a close-on-navigation effect (NavOverlay stays open through back/forward), and adding a zero-consumer handler to a frozen intent-API in the same PR that deletes a zero-consumer prop (DEF-28) is self-contradictory — the slot gets added when a real consumer appears. (2) DEF-38 resolves as drop-the-guard, KEEP the broad catch: narrowing to JSON.parse is a live regression — `'[null]'` in localStorage throws at `item.quantity` OUTSIDE the narrowed try, zustand's `toThenable` swallows the rejection, `hasHydrated` stays false forever, and CheckoutScreen (both visibility flags gate on `useCartHydrated`) renders a permanently bare page with no error anywhere; today's broad catch returns null and hydration completes. `PersistStorage.getItem` is exactly the manifesto-2.4 trust boundary, and the `as ICartItem[]` cast nullifies any type value the guard pretended to add.
- Planner spot-checks at source before ruling, all four confirmed: sheet.tsx composes the consumer `onCloseAutoFocus` BEFORE `restoreOpener` (the preventDefault path already exists — DS signature unchanged); the dialog/sheet scrim literals are byte-identical (`SheetPrimitive.Overlay` IS `DialogPrimitive.Overlay`); the decoder's `.map` sits inside the try with the as-cast; both CheckoutScreen flags hang on `isHydrated`.
- DEF-27 mechanics approved: `closedByNavigationRef` written via `useSidebarStore.getState()` before `close()` (a subscribed `isOpen` in deps would close-on-open; a bare read trips the active exhaustive-deps), consumer preventDefault in CartDrawer, the pathname guard dies. The declared click-then-navigate residual is the DELIVERABLE, not a violation: the setTimeout(0)-vs-route-commit race resolves to today's majority outcome (focus back to the persistent-header opener). DEF-32's `inert` claim is sharper than the prompt knew: `@types/react` already declares `inert?: boolean`, so today `<IconButton inert>` would leak a REAL DOM inert through `{...rest}` and strand focus inside the Radix trap — claiming and destructuring the name closes the hole; `aria-disabled={inert}` (not `inert || undefined`) preserves Lightbox's explicit `"false"` while keeping the attribute off the other consumers.
- Riders on the approval: the CartDrawer navigation-close test must pin the flag-reset sequence (nav-close → reopen → manual close → focus restored; a stuck `closedByNavigationRef` must be a red test); `vi.mock` is sanctioned for FRAMEWORK boundaries only (`next/navigation`) — app modules keep the public-surface doctrine. Coverage honesty logged: DEF-27/28/32 surfaces have zero test hits today (the pathname guard could be deleted with CI green) — the four new test files close that.

## 2026-08-01 — DEF-27 adapt round: planner premise corrected at source; five focus regressions closed; PR #16 delivered

- The first max-review round caught a PLANNER-level error, not an executor one: my gate ratification of §1.4 ("focus returning to the opener after click-then-navigate is today's majority outcome and correct") was made without reading the introducing commit — `300333d` added the pathname guard precisely to STOP restore-after-navigation, calling it a symptom. Five focus regressions confirmed across four surfaces (Proceed/"here" links deterministically yanking focus to the cart button on /checkout — the main conversion path; NavOverlay links → burger on the new page; a double route change inside the 200ms exit animation clobbering the unconsumed flag; the nested ConfirmDialog restoring into a dying subtree; reopen-during-exit sticking the flag so the NEXT manual close dropped focus to body). RULED: option 2, adapt to completion — the old guard was a leaky heuristic that mostly LOST its own setTimeout(0)-vs-route-commit race on the conversion path, so restoring it (option 1) would have reinstated a mechanism that fails its own purpose; explicit intent delivers `300333d`'s semantics deterministically.
- The "Dialog gets no slot" ruling REVERSED on its own trigger condition: the premise was true about effects and false about behavior — NavOverlay's links close-and-navigate in one click, i.e. the "real consumer" the original ruling was waiting for existed all along. This entry is the ratification the review demanded; state.md updated to match the code.
- The second max-review round refined the cure itself: intent declared at click time is a PREDICTION — false on same-route links and Cmd-click — so the declaration is now conditional on the document actually leaving (`willNavigateAway`). The reviewer also probed and killed the trailing-slash hypothesis (Next 16 normalizes `/${locale}/` to `/uk`) and verified the no-clobber composition (the consumer slot runs before `restoreOpener`, and `onCloseAutoFocus` is explicitly extracted from the props spread — losing that would silently drop the lastInteraction fallback for Safari/Firefox).
- PR #16 shipped: the hook lost URL comparison entirely (planner-verified: zero `pathname` references under `src/design-system/`), navigation knowledge lives in app-land `useNavigationClose` (CartDrawer carries TWO instances — the drawer and the nested remove-confirm), refcounted listeners (the module no longer touches `document` on import), `SectionBand.center` gone (the surviving grep hit is the `items-center` utility), IconButton owns `inert`/`aria-disabled` (Lightbox no longer passes the class), one `Scrim` (byte-identical literals merged, the old passthrough sealed), the decoder per DEF-38. 316 units (+51), both PR checks green, the reviewer's independent battery green in a worktree, fence clean.
- Findings routed: (1) the reversal — ratified here. (2) The field-less-payload hole (a valid array like `[{"id","quantity"}]` hydrates into real lines → `price * quantity` is NaN → `"total": null` reaches the order relay) → DEF-39, TOGETHER with (4a) the lying `(item: ICartItem)` annotation on the decode map — validation at the PersistStorage trust boundary (manifesto 2.4) and honest typing are ONE change, not two churns of the same lines. (3) The open-on-add focus test (the DEF-23 origin path — triggerless open, the only opener living outside the header, zero coverage) → in-PR micro-round. (4b) The Scrim's Radix-context coupling (it works inside Sheet because sheet.tsx imports the same `@radix-ui/react-dialog`) → ACCEPTED with rationale: that identity is true by construction today, and a primitive migration would be a decision-level event that revisits the scrim anyway.

## 2026-08-02 — step 6b CLOSED: PR #16 merged, prod live-verified; DEF-40 confirmed and scheduled to step 7

- The open-on-add micro-round landed with an exemplary mutation proof: removing `retainInteractionTracking` reddens ONLY the new case while the other thirteen drawer tests stay green — the zero-coverage claim confirmed experimentally, and the failure symptom (focus in `<body>`) reproduces DEF-23 verbatim. The user's 9-point browser gate passed, including the two animation-window items no machine could check (reopen during exit; Back with ConfirmDialog open). Squash-merged as `7c9f89d`; merged-master battery green (317/317, both TS programs, 46/46); prod deploy READY with pages/sitemap/aria smoke clean. DEF-27/28/32/38 CLOSED with dispositions.
- DEF-40 (user-spotted during the gate, planner-diagnosed at source, user-confirmed experimentally — the veil pulses NON-STOP): the lightbox skeleton never actually hides — `utg-pulse` animates `opacity` infinitely and a running CSS animation beats the normal `opacity-0` declaration in the cascade, while the `absolute` skeleton paints above the static `<img>` (positioned-over-static), so the settled photo pulses behind an opaque `bg-muted` veil on a 1.4s cycle. NOT a #16 delta (ReportsScreen untouched there) — live on prod since step 5; the step-5 gate missed it honestly (its checklist item watched the LOADING moment, where the pulse is the intended look). USER SEQUENCING: the fix rides step 7 as a rider instead of an immediate micro-fix — step 7 starts right away, the delay is small.
- Next: step 7 (README + presentation, framing the initiative system as a feature per D-3, + the DEF-40 rider) runs as the /step skill's PILOT — the first live run of the packaged planner-executor loop; feedback on the skill itself is part of the deliverable.

## 2026-08-02 — step 7 CLOSED: PR #17 merged; the BOARD IS COMPLETE; the /step pilot retro

- The full loop ran in-session for the first time: plan gate (7 questions, all planner-ruled — nothing crossed the product/money/scope tripwires, zero user interruptions, post-hoc digests kept visibility), a 16/16-finding independent review with a hand-run negative control (the regression test really fails on revert; screenshots regenerate byte-identical), a 12-item truth-focused fix round (the acceptance gate "every README claim spot-verifiable" enforced literally — the en screenshot now SHOWS ₴ prices under English chrome instead of claiming them), and planner verification on the final state. Squash-merged as `8591696` by the user after the browser gate. Merged-master battery green (317/317, 46/46); prod deploy READY; smoke clean (the `animate-none` HTML grep is the wrong layer by design — the skeleton mounts only inside the opened lightbox portal; the merge-sha source, CI, and the branch-side visual gate carry the proof).
- DEF-40 CLOSED with a corrected disposition — the planner's `transition-opacity`-restores-the-fade sub-claim was WRONG (the frame trace proves the declared value never changes, so nothing interpolates; the visible fade is the photo's own) — and DEF-41 OPENED from review R4: the DS Skeleton hard-codes its pulse, so hiding it requires consumers to cancel a DS-internal animation — the exact coupling class 6b removed from focus handling; the next DS window owns it. Review acceptances with rationale: R9/R10 (the capture harness reuses e2e-support locators; `EN_HOME_PATH` went public for it) — reuse beats duplication, and breakage surfaces exactly at regeneration time, when someone is looking; R16 (~468 KB of image blobs per regeneration) — accepted, regeneration is deliberate and byte-stable until the UI genuinely changes.
- THE /step PILOT RETRO (material for the parked user discussion):
  1. WORKED: the headless gate protocol (end-turn-as-report) survived two full gates; SendMessage fix rounds kept the executor's context alive across three turns; the agents audited the PLANNER too — two doc defects (the missing D-13 index row, the charter's stale "10 steps") and one wrong sub-claim (the DEF-40 fade mechanism) were caught by the executor/reviewer, not by the planner.
  2. FINDING (user-parked for the retro): /feature's internal review layer does NOT run headless — its Workflow tool is absent in subagents and Skill(code-review) is refused — so the executor pipeline silently degrades to self-review; the independent /review agent is the ONLY real review coverage in this scheme. Discussion options: accept as-is (the independent reviewer is mandatory anyway), have the executor spawn its own reviewer subagent, or teach the feature skill a headless fallback.
  3. FINDING: shared-tree choreography needs one protocol line — the reviewer's sanctioned cleanup (`checkout master`) surfaced as a mystery to the later-resumed executor (an Edit failed on reverted file state; the reflog showed the move; nothing lost). Candidate skill addition: every agent-resume message states the current tree state.
  4. Cost for the record (per-turn token totals from notifications): executor 144k → 239k → 262k, reviewer 141k — heavier than tab sessions, but with zero courier overhead and full planner context at every gate.
- The board is COMPLETE: steps 0–7 and the design track all ✅ across PRs #2–#17. Charter acceptance: tests+CI on every PR ✓, README presents the project ✓, Next 16 + React 19 ✓, prod alive and verified after every merge ✓; the ONE criterion never explicitly measured is Lighthouse mobile performance/accessibility ≥ 90 (SEO 100 was user-verified in step 5) — a user devtools run on home/category/product closes the charter honestly. The bot-polish phase (DEF-13) is UNPARKED by its own condition: the app work (steps 5–7) is complete.

## 2026-08-02 — CHARTER SATISFIED

- The user ran and accepted the Lighthouse mobile performance/accessibility check — the last unmeasured criterion (SEO 100 stood since step 5). Every acceptance criterion in charter.md is now met, across PRs #2–#17, with prod live-verified after every merge. The initiative is functionally COMPLETE; the /step pilot retro and close-out housekeeping run in-session; the bot-polish phase awaits the user's opening.
